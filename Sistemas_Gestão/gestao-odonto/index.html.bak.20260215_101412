<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Plena Odonto - Gestão Odontológica</title>

    <meta name="theme-color" content="#0E7490">
    <meta name="msapplication-TileColor" content="#0E7490">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Plena Odonto">

    <link rel="manifest"
        href='data:application/manifest+json,{"name":"Plena Odonto","short_name":"Plena Odonto","start_url":".","display":"standalone","background_color":"#F0F9FF","theme_color":"#0E7490","icons":[{"src":"data:image/svg+xml;base64,","sizes":"192x192","type":"image/svg+xml"}]}'>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        plena: {
                            blue: '#0E7490',
                            teal: '#14B8A6',
                            cyan: '#06B6D4',
                            dark: '#164E63',
                            lightblue: '#22D3EE',
                            purple: '#8B5CF6',
                            pink: '#EC4899',
                            green: '#10B981',
                            amber: '#F59E0B',
                            red: '#EF4444'
                        },
                    },
                    animation: {
                        'pulse-slow': 'pulse 3s infinite',
                        'slide-in': 'slideIn 0.3s ease-out',
                        'fade-in': 'fadeIn 0.5s ease-out',
                    }
                }
            }
        }
    </script>

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@0.378.0/dist/umd/lucide.js"></script>

    <style>
        /* CSS Otimizado - Mantendo estrutura similar */
        :root {
            --plena-blue: #0E7490;
            --plena-teal: #14B8A6;
            --plena-cyan: #06B6D4;
            --plena-dark: #164E63;
            --plena-lightblue: #22D3EE;
            --plena-purple: #8B5CF6;
            --plena-pink: #EC4899;
            --plena-green: #10B981;
            --plena-amber: '#F59E0B';
            --plena-red: #EF4444;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: #F0F9FF;
            -webkit-tap-highlight-color: transparent;
        }

        .hide {
            display: none !important;
        }

        .fade-in {
            animation: fadeIn 0.3s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(5px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            from {
                transform: translateX(-10px);
                opacity: 0;
            }

            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        /* Modal Backdrop */
        .modal-backdrop {
            background-color: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(4px);
            animation: fadeIn 0.2s ease;
        }

        /* Sidebar Mobile */
        @media (max-width: 1024px) {
            .sidebar {
                transform: translateX(-100%);
                position: fixed;
                z-index: 50;
                height: 100vh;
                width: 280px;
            }

            .sidebar.open {
                transform: translateX(0);
            }
        }

        @media (min-width: 1025px) {
            .sidebar {
                width: 280px;
                position: fixed;
                height: 100vh;
            }

            .main-content {
                margin-left: 280px;
            }
        }

        /* Card Hover Effects */
        .card-hover {
            transition: all 0.3s ease;
        }

        .card-hover:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }

        /* Badges */
        .badge {
            display: inline-flex;
            align-items: center;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-success {
            background-color: #DCFCE7;
            color: #166534;
        }

        .badge-warning {
            background-color: #FEF3C7;
            color: #92400E;
        }

        .badge-danger {
            background-color: #FEE2E2;
            color: #991B1B;
        }

        .badge-info {
            background-color: #DBEAFE;
            color: #1E40AF;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 3px;
        }

        ::-webkit-scrollbar-thumb {
            background: #c1c1c1;
            border-radius: 3px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #a1a1a1;
        }

        /* Status Indicators */
        .status-indicator {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            display: inline-block;
            margin-right: 8px;
        }

        .status-agendado {
            background-color: #3B82F6;
        }

        .status-confirmado {
            background-color: #10B981;
        }

        .status-em-atendimento {
            background-color: #F59E0B;
        }

        .status-concluido {
            background-color: #8B5CF6;
        }

        .status-cancelado {
            background-color: #EF4444;
        }

        .status-falta {
            background-color: #6B7280;
        }

        /* Progress bar */
        .progress-bar {
            height: 6px;
            border-radius: 3px;
            background: #e5e7eb;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #0E7490, #14B8A6);
            transition: width 0.5s ease;
        }

        /* Print Styles */
        @media print {
            @page {
                size: A4;
                margin: 0;
            }

            body {
                background: white !important;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
                height: 100vh;
                margin: 0;
                padding: 0;
            }

            body>* {
                display: none !important;
            }

            #receiptModal {
                display: flex !important;
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                margin: 0;
                padding: 0;
                background: white;
                z-index: 9999;
                align-items: flex-start;
                justify-content: center;
                overflow: visible !important;
            }

            #receiptModal .bg-white {
                box-shadow: none !important;
                max-width: 100% !important;
                width: 100% !important;
                padding: 0 !important;
                margin: 0 !important;
            }

            #receipt-content {
                padding: 40px !important;
                width: 100%;
                font-size: 12pt;
            }

            .no-print {
                display: none !important;
            }
        }

        /* Timeline */
        .timeline-item {
            position: relative;
            padding-left: 30px;
            margin-bottom: 20px;
        }

        .timeline-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 5px;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background-color: #0E7490;
            border: 3px solid white;
            box-shadow: 0 0 0 3px #0E7490;
        }

        .timeline-item::after {
            content: '';
            position: absolute;
            left: 5px;
            top: 23px;
            width: 2px;
            height: calc(100% + 5px);
            background-color: #e5e7eb;
        }

        .timeline-item:last-child::after {
            display: none;
        }
    </style>
</head>

<body class="text-gray-800 antialiased">
    <!-- Overlay para sidebar mobile -->
    <div id="overlay" class="fixed inset-0 bg-black/50 hidden z-40 transition-opacity" onclick="toggleSidebar()"></div>

    <!-- Sidebar -->
    <aside id="sidebar"
        class="sidebar bg-gradient-to-b from-plena-dark to-gray-900 text-white transition-transform duration-300 flex flex-col shadow-2xl">
        <div class="h-20 flex items-center px-6 bg-gradient-to-r from-plena-blue to-plena-teal">
            <i data-lucide="tooth" class="w-8 h-8 mr-3"></i>
            <div>
                <h1 class="text-xl font-bold tracking-tight">PLENA ODONTO</h1>
                <p class="text-xs text-white/90 font-medium">Gestão Odontológica Inteligente</p>
            </div>
        </div>

        <nav class="p-4 space-y-2 flex-1 overflow-y-auto">
            <button onclick="router('dashboard')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group active-nav"
                id="nav-dashboard">
                <i data-lucide="layout-dashboard" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Dashboard
            </button>
            <button onclick="router('schedule')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-schedule">
                <i data-lucide="calendar" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Agenda
            </button>
            <button onclick="router('patients')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-patients">
                <i data-lucide="users" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Pacientes
            </button>
            <button onclick="router('treatments')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-treatments">
                <i data-lucide="activity" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Tratamentos
            </button>
            <button onclick="router('financial')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-financial">
                <i data-lucide="wallet" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Financeiro
            </button>
            <button onclick="router('stock')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-stock">
                <i data-lucide="package" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Estoque
            </button>
            <button onclick="router('reports')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-reports">
                <i data-lucide="bar-chart-3" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Relatórios
            </button>
            <button onclick="router('manual')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-manual">
                <i data-lucide="book-open" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Manual de Uso
            </button>
            <button onclick="router('settings')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-settings">
                <i data-lucide="settings" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Configurações
            </button>

            <button onclick="router('about')"
                class="nav-item w-full flex items-center px-4 py-3 rounded-xl hover:bg-white/10 transition-all group"
                id="nav-about">
                <i data-lucide="info" class="w-5 h-5 mr-3 text-gray-400 group-hover:text-white"></i>
                Sobre a Plena
            </button>
        </nav>

        <div class="p-6 border-t border-white/10 text-center">
            <p class="text-xs text-gray-500">Todos os Direitos Reservados &copy; 2026</p>
            <a href="https://www.plenaaplicativos.com.br" target="_blank"
                class="text-[10px] text-gray-600 hover:text-plena-blue transition-colors mt-1 block decoration-0">
                Plena Soluções Digitais LTDA
            </a>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content min-h-screen flex flex-col">
        <!-- Header -->
        <header
            class="bg-white h-20 flex items-center justify-between px-4 lg:px-8 sticky top-0 z-30 shadow-sm border-b border-gray-100">
            <div class="flex items-center gap-4">
                <button onclick="toggleSidebar()" class="lg:hidden p-2 hover:bg-gray-100 rounded-lg text-gray-600">
                    <i data-lucide="menu" class="w-6 h-6"></i>
                </button>
                <h2 id="page-title" class="text-xl font-bold text-gray-800 hidden sm:block">Dashboard</h2>
            </div>

            <div class="flex items-center gap-2 sm:gap-3">
                <button onclick="openAppointmentModal()"
                    class="relative flex items-center px-4 py-2.5 bg-gradient-to-r from-plena-blue to-plena-teal text-white rounded-lg text-sm font-bold hover:opacity-90 transition-all shadow-lg shadow-cyan-200 active:scale-95">
                    <i data-lucide="plus" class="w-5 h-5 sm:mr-2"></i>
                    <span class="hidden sm:inline">Novo Agendamento</span>
                    <span class="sm:hidden">Novo</span>
                </button>
            </div>
        </header>

        <!-- Content Area -->
        <div class="p-4 lg:p-8 flex-1 overflow-y-auto">
            <!-- Dashboard View -->
            <section id="view-dashboard" class="view-section fade-in">
                <!-- Quick Stats -->
                <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 card-hover">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-1">Agendamentos
                                    Hoje</p>
                                <h3 id="dash-today" class="text-3xl font-bold text-gray-900">0</h3>
                                <p class="text-xs text-gray-500 mt-1">Consultas programadas</p>
                            </div>
                            <div class="w-12 h-12 rounded-full bg-cyan-100 flex items-center justify-center">
                                <i data-lucide="calendar" class="w-6 h-6 text-cyan-600"></i>
                            </div>
                        </div>
                    </div>
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 card-hover">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-1">Pacientes
                                    Ativos</p>
                                <h3 id="dash-patients" class="text-3xl font-bold text-gray-900">0</h3>
                                <p class="text-xs text-gray-500 mt-1">Em tratamento</p>
                            </div>
                            <div class="w-12 h-12 rounded-full bg-green-100 flex items-center justify-center">
                                <i data-lucide="users" class="w-6 h-6 text-green-600"></i>
                            </div>
                        </div>
                    </div>
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 card-hover">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-1">Receita Mensal
                                </p>
                                <h3 id="dash-revenue" class="text-2xl font-bold text-green-600">R$ 0,00</h3>
                                <p class="text-xs text-gray-500 mt-1">
                                    <span id="revenue-growth" class="text-green-500">+0%</span> vs último mês
                                </p>
                            </div>
                            <div class="w-12 h-12 rounded-full bg-green-100 flex items-center justify-center">
                                <i data-lucide="trending-up" class="w-6 h-6 text-green-600"></i>
                            </div>
                        </div>
                    </div>
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 card-hover">
                        <div class="flex items-center justify-between">
                            <div>
                                <p class="text-xs font-bold text-gray-400 uppercase tracking-wider mb-1">Próximas
                                    Consultas</p>
                                <h3 id="dash-next" class="text-2xl font-bold text-plena-blue">0</h3>
                                <p class="text-xs text-gray-500 mt-1">Próximos 7 dias</p>
                            </div>
                            <div class="w-12 h-12 rounded-full bg-blue-100 flex items-center justify-center">
                                <i data-lucide="clock" class="w-6 h-6 text-blue-600"></i>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Two Column Layout -->
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                    <!-- Today's Schedule -->
                    <div class="lg:col-span-2 bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <div class="flex justify-between items-center mb-6">
                            <h3 class="font-bold text-gray-800">Agenda de Hoje</h3>
                            <div class="flex items-center gap-4">
                                <div class="flex items-center gap-2">
                                    <div class="status-indicator status-agendado"></div>
                                    <span class="text-xs text-gray-600">Agendado</span>
                                </div>
                                <div class="flex items-center gap-2">
                                    <div class="status-indicator status-confirmado"></div>
                                    <span class="text-xs text-gray-600">Confirmado</span>
                                </div>
                            </div>
                        </div>
                        <div id="today-schedule" class="space-y-3">
                            <!-- Dynamic content -->
                        </div>
                    </div>

                    <!-- Quick Actions & Stats -->
                    <div class="space-y-6">
                        <!-- Quick Actions -->
                        <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                            <h3 class="font-bold text-gray-800 mb-4">Ações Rápidas</h3>
                            <div class="grid grid-cols-2 gap-3">
                                <button onclick="router('patients'); openPatientModal();"
                                    class="bg-blue-50 hover:bg-blue-100 p-4 rounded-xl text-center transition-colors">
                                    <i data-lucide="user-plus" class="w-6 h-6 text-blue-600 mx-auto mb-2"></i>
                                    <span class="text-sm font-medium text-blue-700">Novo Paciente</span>
                                </button>
                                <button onclick="openFinancialModal()"
                                    class="bg-green-50 hover:bg-green-100 p-4 rounded-xl text-center transition-colors">
                                    <i data-lucide="dollar-sign" class="w-6 h-6 text-green-600 mx-auto mb-2"></i>
                                    <span class="text-sm font-medium text-green-700">Receber</span>
                                </button>
                                <button onclick="router('stock')"
                                    class="bg-amber-50 hover:bg-amber-100 p-4 rounded-xl text-center transition-colors">
                                    <i data-lucide="package" class="w-6 h-6 text-amber-600 mx-auto mb-2"></i>
                                    <span class="text-sm font-medium text-amber-700">Estoque</span>
                                </button>
                                <button onclick="router('reports')"
                                    class="bg-purple-50 hover:bg-purple-100 p-4 rounded-xl text-center transition-colors">
                                    <i data-lucide="file-text" class="w-6 h-6 text-purple-600 mx-auto mb-2"></i>
                                    <span class="text-sm font-medium text-purple-700">Relatório</span>
                                </button>
                            </div>
                        </div>

                        <!-- Treatment Progress -->
                        <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                            <h3 class="font-bold text-gray-800 mb-4">Tratamentos em Andamento</h3>
                            <div id="treatment-progress" class="space-y-3">
                                <!-- Dynamic content -->
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Schedule View -->
            <section id="view-schedule" class="view-section hide fade-in">
                <div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-gray-800">Agenda</h2>
                        <p class="text-sm text-gray-600">Gerencie consultas e atendimentos</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <div class="relative">
                            <i data-lucide="calendar"
                                class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-plena-blue"></i>
                            <input type="date" id="schedule-date" onchange="renderSchedule()"
                                class="pl-10 pr-4 py-2.5 rounded-lg border-2 border-plena-blue/20 text-sm bg-plena-blue/5 font-bold text-plena-dark focus:border-plena-blue outline-none">
                        </div>
                        <button onclick="openAppointmentModal()"
                            class="flex items-center gap-2 px-4 py-2.5 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-colors">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                            Nova Consulta
                        </button>
                    </div>
                </div>

                <!-- Schedule Timeline -->
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                    <div class="p-6">
                        <div id="schedule-timeline" class="space-y-4">
                            <!-- Dynamic timeline will be inserted here -->
                        </div>
                    </div>
                </div>
            </section>

            <!-- Patients View -->
            <section id="view-patients" class="view-section hide fade-in">
                <div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-gray-800">Pacientes</h2>
                        <p class="text-sm text-gray-600">Gerencie todos os pacientes</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <div class="relative">
                            <i data-lucide="search"
                                class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400"></i>
                            <input type="text" id="search-patients" onkeyup="renderPatients()"
                                placeholder="Buscar paciente..."
                                class="pl-10 pr-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-plena-blue outline-none text-sm">
                        </div>
                        <button onclick="openPatientModal()"
                            class="flex items-center gap-2 px-4 py-2.5 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-colors">
                            <i data-lucide="user-plus" class="w-4 h-4"></i>
                            Novo Paciente
                        </button>
                    </div>
                </div>

                <!-- Patients Grid -->
                <div id="patients-list" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <!-- Dynamic content -->
                </div>
            </section>

            <!-- Treatments View -->
            <section id="view-treatments" class="view-section hide fade-in">
                <div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-gray-800">Tratamentos</h2>
                        <p class="text-sm text-gray-600">Gerencie planos de tratamento</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <button onclick="openTreatmentModal()"
                            class="flex items-center gap-2 px-4 py-2.5 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-colors">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                            Novo Tratamento
                        </button>
                    </div>
                </div>

                <!-- Treatments List -->
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                    <div class="overflow-x-auto">
                        <table class="w-full text-left">
                            <thead class="bg-gray-50 text-gray-500 text-xs uppercase">
                                <tr>
                                    <th class="px-6 py-3">Paciente</th>
                                    <th class="px-6 py-3">Tratamento</th>
                                    <th class="px-6 py-3">Status</th>
                                    <th class="px-6 py-3">Valor</th>
                                    <th class="px-6 py-3">Progresso</th>
                                    <th class="px-6 py-3 text-center">Ações</th>
                                </tr>
                            </thead>
                            <tbody id="treatments-table-body" class="divide-y divide-gray-100 text-sm">
                                <!-- Dynamic content -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </section>

            <!-- Financial View -->
            <section id="view-financial" class="view-section hide fade-in">
                <div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-gray-800">Financeiro</h2>
                        <p class="text-sm text-gray-600">Controle de receitas e despesas</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <button onclick="openFinancialModal()"
                            class="flex items-center gap-2 px-4 py-2.5 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-colors">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                            Nova Movimentação
                        </button>
                    </div>
                </div>

                <!-- Financial Summary -->
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                    <div class="bg-green-50 p-6 rounded-xl border border-green-100">
                        <p class="text-xs text-gray-500 uppercase font-bold mb-1">Receitas do Mês</p>
                        <p id="finance-income" class="text-2xl font-bold text-green-700">R$ 0,00</p>
                    </div>
                    <div class="bg-red-50 p-6 rounded-xl border border-red-100">
                        <p class="text-xs text-gray-500 uppercase font-bold mb-1">Despesas do Mês</p>
                        <p id="finance-expense" class="text-2xl font-bold text-red-700">R$ 0,00</p>
                    </div>
                    <div class="bg-blue-50 p-6 rounded-xl border border-blue-100">
                        <p class="text-xs text-gray-500 uppercase font-bold mb-1">Saldo Total</p>
                        <p id="finance-balance" class="text-2xl font-bold text-blue-700">R$ 0,00</p>
                    </div>
                </div>

                <!-- Transactions Table -->
                <div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">
                    <div class="overflow-x-auto">
                        <table class="w-full text-left">
                            <thead class="bg-gray-50 text-gray-500 text-xs uppercase">
                                <tr>
                                    <th class="px-6 py-3">Data</th>
                                    <th class="px-6 py-3">Descrição</th>
                                    <th class="px-6 py-3">Paciente</th>
                                    <th class="px-6 py-3">Tipo</th>
                                    <th class="px-6 py-3 text-right">Valor</th>
                                    <th class="px-6 py-3 text-center">Ações</th>
                                </tr>
                            </thead>
                            <tbody id="finance-table-body" class="divide-y divide-gray-100 text-sm">
                                <!-- Dynamic content -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </section>

            <!-- Stock View -->
            <section id="view-stock" class="view-section hide fade-in">
                <div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-gray-800">Estoque</h2>
                        <p class="text-sm text-gray-600">Controle de materiais e medicamentos</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <button onclick="openStockModal()"
                            class="flex items-center gap-2 px-4 py-2.5 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-colors">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                            Novo Item
                        </button>
                    </div>
                </div>

                <!-- Stock Alert -->
                <div class="bg-red-50 border border-red-200 rounded-xl p-4 mb-6">
                    <div class="flex items-center gap-3">
                        <i data-lucide="alert-triangle" class="w-5 h-5 text-red-600"></i>
                        <div>
                            <p class="text-sm font-bold text-red-800">Atenção: Itens com estoque baixo</p>
                            <p class="text-xs text-red-600" id="low-stock-count">0 itens precisam de reposição</p>
                        </div>
                    </div>
                </div>

                <!-- Stock Items -->
                <div id="stock-items" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <!-- Dynamic content -->
                </div>
            </section>

            <!-- Reports View -->
            <section id="view-reports" class="view-section hide fade-in">
                <div class="mb-6">
                    <h2 class="text-xl font-bold text-gray-800 mb-2">Relatórios</h2>
                    <p class="text-sm text-gray-600">Análise e estatísticas da clínica</p>
                </div>

                <!-- Report Cards -->
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <div class="text-center">
                            <div
                                class="w-12 h-12 rounded-full bg-blue-100 flex items-center justify-center mx-auto mb-3">
                                <i data-lucide="trending-up" class="w-6 h-6 text-blue-600"></i>
                            </div>
                            <p class="text-xs text-gray-500 uppercase font-bold">Faturamento Mensal</p>
                            <p id="report-revenue" class="text-2xl font-bold text-gray-800 mt-2">R$ 0,00</p>
                        </div>
                    </div>
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <div class="text-center">
                            <div
                                class="w-12 h-12 rounded-full bg-green-100 flex items-center justify-center mx-auto mb-3">
                                <i data-lucide="users" class="w-6 h-6 text-green-600"></i>
                            </div>
                            <p class="text-xs text-gray-500 uppercase font-bold">Novos Pacientes</p>
                            <p id="report-new-patients" class="text-2xl font-bold text-gray-800 mt-2">0</p>
                        </div>
                    </div>
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <div class="text-center">
                            <div
                                class="w-12 h-12 rounded-full bg-amber-100 flex items-center justify-center mx-auto mb-3">
                                <i data-lucide="calendar" class="w-6 h-6 text-amber-600"></i>
                            </div>
                            <p class="text-xs text-gray-500 uppercase font-bold">Taxa de Comparecimento</p>
                            <p id="report-attendance" class="text-2xl font-bold text-gray-800 mt-2">0%</p>
                        </div>
                    </div>
                </div>

                <!-- Report Actions -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <button onclick="generateReport('monthly')"
                        class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow text-left">
                        <i data-lucide="file-text" class="w-8 h-8 text-plena-blue mb-3"></i>
                        <h3 class="font-bold text-gray-800 mb-2">Relatório Mensal</h3>
                        <p class="text-sm text-gray-600">Faturamento, despesas e resultados do mês</p>
                    </button>
                    <button onclick="generateReport('treatments')"
                        class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow text-left">
                        <i data-lucide="activity" class="w-8 h-8 text-green-600 mb-3"></i>
                        <h3 class="font-bold text-gray-800 mb-2">Relatório de Tratamentos</h3>
                        <p class="text-sm text-gray-600">Status e progresso dos tratamentos ativos</p>
                    </button>
                </div>
            </section>

            <!-- Settings View -->
            <section id="view-settings" class="view-section hide fade-in">
                <div class="mb-6 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h2 class="text-xl font-bold text-gray-800">Configurações</h2>
                        <p class="text-sm text-gray-600">Personalize sua clínica</p>
                    </div>
                    <button onclick="saveSettings()"
                        class="flex items-center gap-2 px-6 py-2.5 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-colors shadow-lg shadow-blue-200">
                        <i data-lucide="save" class="w-4 h-4"></i>
                        Salvar Alterações
                    </button>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                    <!-- Clinic Data -->
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <h3 class="font-bold text-gray-800 mb-4 flex items-center">
                            <i data-lucide="home" class="w-5 h-5 mr-2 text-plena-blue"></i>
                            Dados da Clínica
                        </h3>
                        <div class="space-y-4">
                            <div>
                                <label class="text-xs font-bold text-gray-500 uppercase">Nome da Clínica</label>
                                <input type="text" id="clinic-name"
                                    class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none"
                                    placeholder="Ex: Sorriso Perfeito Odontologia">
                            </div>
                            <div>
                                <label class="text-xs font-bold text-gray-500 uppercase">Responsável Técnico
                                    (CRO)</label>
                                <input type="text" id="dentist-cro"
                                    class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none"
                                    placeholder="CRO-XXXXX">
                            </div>
                            <div>
                                <label class="text-xs font-bold text-gray-500 uppercase">Telefone</label>
                                <input type="text" id="clinic-phone"
                                    class="mask-phone w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none"
                                    placeholder="(00) 00000-0000">
                            </div>
                        </div>
                    </div>

                    <!-- Treatment Settings -->
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <h3 class="font-bold text-gray-800 mb-4 flex items-center">
                            <i data-lucide="activity" class="w-5 h-5 mr-2 text-green-600"></i>
                            Configurações de Tratamento
                        </h3>
                        <div class="space-y-4">
                            <div>
                                <label class="text-xs font-bold text-gray-500 uppercase">Tempo Padrão de Consulta
                                    (min)</label>
                                <input type="number" id="appointment-duration" value="30"
                                    class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                            </div>
                            <div>
                                <label class="text-xs font-bold text-gray-500 uppercase">Lembrete por WhatsApp (horas
                                    antes)</label>
                                <input type="number" id="reminder-hours" value="24"
                                    class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                            </div>
                        </div>
                    </div>

                    <!-- Backup Section -->
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <h3 class="font-bold text-gray-800 mb-4 flex items-center">
                            <i data-lucide="database" class="w-5 h-5 mr-2 text-amber-600"></i>
                            Backup e Restauração
                        </h3>
                        <div class="space-y-3">
                            <button onclick="downloadBackup()"
                                class="w-full border border-gray-300 py-3 rounded-lg text-sm font-medium hover:bg-gray-50 flex justify-center items-center gap-2">
                                <i data-lucide="download" class="w-4 h-4"></i>
                                Exportar Dados
                            </button>
                            <div class="relative">
                                <button
                                    class="w-full bg-gray-800 text-white py-3 rounded-lg text-sm font-medium flex justify-center items-center gap-2">
                                    <i data-lucide="upload" class="w-4 h-4"></i>
                                    Importar Dados
                                </button>
                                <input type="file" onchange="restoreBackup(this)" accept=".json"
                                    class="absolute inset-0 opacity-0 cursor-pointer">
                            </div>
                        </div>
                    </div>

                    <!-- Danger Zone -->
                    <div class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">
                        <h3 class="font-bold text-gray-800 mb-4 flex items-center text-red-600">
                            <i data-lucide="alert-triangle" class="w-5 h-5 mr-2"></i>
                            Área de Risco
                        </h3>
                        <div class="grid grid-cols-2 gap-3">
                            <button onclick="clearAllData()"
                                class="py-2.5 border border-red-300 text-red-600 rounded-lg text-sm hover:bg-red-50 transition-colors">
                                Limpar Todos Dados
                            </button>
                            <button onclick="factoryReset()"
                                class="py-2.5 bg-red-600 text-white rounded-lg text-sm hover:bg-red-700 transition-colors">
                                Resetar Sistema
                            </button>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Manual View -->
            <section id="view-manual" class="view-section hide fade-in">
                <!-- Manual Hero -->
                <div
                    class="bg-gradient-to-r from-plena-blue to-plena-teal rounded-3xl p-8 text-white mb-8 relative overflow-hidden shadow-lg shadow-cyan-900/20">
                    <div class="relative z-10 grid md:grid-cols-2 gap-8 items-center">
                        <div>
                            <div
                                class="inline-flex items-center gap-2 bg-white/20 backdrop-blur-md px-4 py-1.5 rounded-full text-sm font-medium mb-4 border border-white/30">
                                <i data-lucide="graduation-cap" class="w-4 h-4 text-white"></i>
                                <span>Central de Aprendizado</span>
                            </div>
                            <h1 class="text-4xl font-bold mb-4 leading-tight">Domine o Plena Odonto</h1>
                            <p class="text-white/90 text-lg mb-6 leading-relaxed">
                                Guia interativo completo para maximizar a gestão do seu consultório.
                                Aprenda a gerenciar pacientes, agendamentos, tratamentos e finanças.
                            </p>

                            <!-- Progress Bar -->
                            <div class="bg-black/20 rounded-xl p-4 backdrop-blur-sm border border-white/10">
                                <div class="flex justify-between text-sm mb-2 font-medium">
                                    <span class="flex items-center gap-2">
                                        <i data-lucide="book-open" class="w-4 h-4"></i>
                                        Progresso do Treinamento
                                    </span>
                                    <span id="manual-progress-text">0% Concluído</span>
                                </div>
                                <div class="h-3 bg-black/20 rounded-full overflow-hidden">
                                    <div id="manual-progress-bar"
                                        class="h-full bg-white transition-all duration-700 ease-out" style="width: 0%">
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="hidden md:flex justify-center">
                            <i data-lucide="tooth"
                                class="w-64 h-64 text-white/20 absolute -right-10 -bottom-10 rotate-12"></i>
                            <div
                                class="relative bg-white/10 backdrop-blur-md p-6 rounded-2xl border border-white/20 transform rotate-3 hover:rotate-0 transition-transform duration-500">
                                <div class="flex items-center gap-4 mb-4 border-b border-white/10 pb-4">
                                    <div class="w-12 h-12 rounded-full bg-white/20 flex items-center justify-center">
                                        <i data-lucide="check-circle-2" class="w-6 h-6 text-white"></i>
                                    </div>
                                    <div>
                                        <p class="font-bold text-lg">Checklist de Início</p>
                                        <p class="text-xs text-white/70">Passos essenciais</p>
                                    </div>
                                </div>
                                <ul class="space-y-3 text-sm">
                                    <li class="flex items-center gap-3">
                                        <i data-lucide="check" class="w-4 h-4 text-green-300"></i>
                                        <span>Configurar Clínica</span>
                                    </li>
                                    <li class="flex items-center gap-3">
                                        <i data-lucide="check" class="w-4 h-4 text-green-300"></i>
                                        <span>Cadastrar 1º Paciente</span>
                                    </li>
                                    <li class="flex items-center gap-3">
                                        <i data-lucide="check" class="w-4 h-4 text-green-300"></i>
                                        <span>Criar Agendamento</span>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Navigation Grid -->
                <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
                    <button onclick="scrollToManualSection('manual-install')"
                        class="bg-white p-4 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-all text-center group">
                        <div
                            class="w-10 h-10 rounded-full bg-blue-50 text-blue-600 mx-auto mb-3 flex items-center justify-center group-hover:scale-110 transition-transform">
                            <i data-lucide="download" class="w-5 h-5"></i>
                        </div>
                        <span class="font-bold text-gray-700 text-sm">Instalação</span>
                    </button>
                    <button onclick="scrollToManualSection('manual-patients')"
                        class="bg-white p-4 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-all text-center group">
                        <div
                            class="w-10 h-10 rounded-full bg-green-50 text-green-600 mx-auto mb-3 flex items-center justify-center group-hover:scale-110 transition-transform">
                            <i data-lucide="users" class="w-5 h-5"></i>
                        </div>
                        <span class="font-bold text-gray-700 text-sm">Pacientes</span>
                    </button>
                    <button onclick="scrollToManualSection('manual-agenda')"
                        class="bg-white p-4 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-all text-center group">
                        <div
                            class="w-10 h-10 rounded-full bg-amber-50 text-amber-600 mx-auto mb-3 flex items-center justify-center group-hover:scale-110 transition-transform">
                            <i data-lucide="calendar" class="w-5 h-5"></i>
                        </div>
                        <span class="font-bold text-gray-700 text-sm">Agenda</span>
                    </button>
                    <button onclick="scrollToManualSection('manual-treatments')"
                        class="bg-white p-4 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-all text-center group">
                        <div
                            class="w-10 h-10 rounded-full bg-purple-50 text-purple-600 mx-auto mb-3 flex items-center justify-center group-hover:scale-110 transition-transform">
                            <i data-lucide="activity" class="w-5 h-5"></i>
                        </div>
                        <span class="font-bold text-gray-700 text-sm">Tratamentos</span>
                    </button>
                </div>

                <!-- Manual Content Sections -->
                <div class="space-y-6">
                    <!-- 1. Instalação -->
                    <div id="manual-install"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    1</div>
                                <h3 class="text-lg font-bold text-gray-800">Instalação e Acesso (PWA)</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p class="mb-4">O <strong>Plena Odonto</strong> é um aplicativo Web Progressivo (PWA), o
                                    que significa que você pode instalá-lo diretamente no seu dispositivo sem precisar
                                    de lojas de aplicativos.</p>

                                <div class="grid md:grid-cols-2 gap-6 mt-4">
                                    <div class="bg-gray-50 p-4 rounded-xl border border-gray-200">
                                        <h4 class="font-bold text-gray-800 mb-2 flex items-center gap-2">
                                            <i data-lucide="smartphone" class="w-4 h-4 text-blue-500"></i> No Celular
                                            (Android/iOS)
                                        </h4>
                                        <ul class="list-disc pl-5 space-y-1">
                                            <li>Abra o link no navegador <strong>Chrome</strong> (Android) ou
                                                <strong>Safari</strong> (iOS).
                                            </li>
                                            <li>Toque no menu de opções (três pontos ou botão de compartilhamento).</li>
                                            <li>Selecione <strong>"Adicionar à Tela de Início"</strong>.</li>
                                        </ul>
                                    </div>
                                    <div class="bg-gray-50 p-4 rounded-xl border border-gray-200">
                                        <h4 class="font-bold text-gray-800 mb-2 flex items-center gap-2">
                                            <i data-lucide="laptop" class="w-4 h-4 text-blue-500"></i> No Computador
                                        </h4>
                                        <ul class="list-disc pl-5 space-y-1">
                                            <li>Abra no <strong>Google Chrome</strong> ou <strong>Edge</strong>.</li>
                                            <li>Clique no ícone de instalação <i data-lucide="download"
                                                    class="w-3 h-3 inline"></i> na barra de endereço.</li>
                                            <li>Confirme clicando em "Instalar".</li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="install" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>

                    <!-- 2. Configuração Inicial -->
                    <div id="manual-config"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    2</div>
                                <h3 class="text-lg font-bold text-gray-800">Configurações Iniciais</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p>Antes de começar a atender, configure os dados do seu consultório:</p>
                                <ol class="list-decimal pl-5 space-y-2 mt-2">
                                    <li>Acesse o menu <strong>Configurações</strong>.</li>
                                    <li>Preencha o <strong>Nome da Clínica</strong> e o <strong>CRO do Responsável
                                            Técnico</strong>.</li>
                                    <li>Defina o <strong>Tempo Padrão de Consulta</strong> (ex: 30 minutos). Isso
                                        ajustará automaticamente a duração dos agendamentos.</li>
                                    <li>Configure o <strong>Lembrete de WhatsApp</strong> (padrão 24h) para avisar
                                        pacientes antes das consultas.</li>
                                </ol>
                                <div
                                    class="bg-amber-50 p-3 rounded-lg border border-amber-200 mt-4 flex gap-3 text-amber-800 text-xs">
                                    <i data-lucide="alert-circle" class="w-5 h-5 flex-shrink-0"></i>
                                    <p>Estes dados aparecerão nos relatórios e recibos gerados pelo sistema.</p>
                                </div>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="config" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>

                    <!-- 3. Gestão de Pacientes -->
                    <div id="manual-patients"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    3</div>
                                <h3 class="text-lg font-bold text-gray-800">Gestão de Pacientes</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p>Centralize o histórico dos seus pacientes:</p>
                                <ul class="list-disc pl-5 space-y-2 mt-2">
                                    <li>Clique em <strong>Novo Paciente</strong> no Dashboard ou na aba Pacientes.</li>
                                    <li>Preencha os dados obrigatórios (Nome, WhatsApp).</li>
                                    <li>O campo <strong>CPF</strong> é importante para emissão de recibos.</li>
                                    <li>Use o campo <strong>Observações</strong> para anotar alergias ou dados da
                                        anamnese simplificada.</li>
                                    <li>Na lista de pacientes, você pode editar dados, ver o histórico e agendar
                                        diretamente clicando no botão "Agendar".</li>
                                </ul>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="patients" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>

                    <!-- 4. Agenda Inteligente -->
                    <div id="manual-agenda"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    4</div>
                                <h3 class="text-lg font-bold text-gray-800">Agenda e Consultas</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p>Organize seu dia com a agenda interativa:</p>
                                <ul class="list-disc pl-5 space-y-2 mt-2">
                                    <li><strong>Novo Agendamento</strong>: Selecione o paciente, data, hora e
                                        procedimento.</li>
                                    <li><strong>Cores de Status</strong>:
                                        <div class="grid grid-cols-2 gap-2 mt-2 text-xs">
                                            <span
                                                class="px-2 py-1 bg-blue-100 text-blue-700 rounded w-fit">Agendado</span>
                                            <span
                                                class="px-2 py-1 bg-green-100 text-green-700 rounded w-fit">Confirmado</span>
                                            <span class="px-2 py-1 bg-amber-100 text-amber-700 rounded w-fit">Em
                                                Atendimento</span>
                                            <span
                                                class="px-2 py-1 bg-purple-100 text-purple-700 rounded w-fit">Concluído</span>
                                            <span
                                                class="px-2 py-1 bg-red-100 text-red-700 rounded w-fit">Cancelado</span>
                                            <span class="px-2 py-1 bg-gray-100 text-gray-700 rounded w-fit">Falta</span>
                                        </div>
                                    </li>
                                    <li><strong>Alterar Status</strong>: Na agenda ou dashboard, clique no botão de
                                        status para avançar o ciclo (ex: de Agendado para Confirmado).</li>
                                </ul>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="agenda" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>

                    <!-- 5. Tratamentos -->
                    <div id="manual-treatments"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    5</div>
                                <h3 class="text-lg font-bold text-gray-800">Planos de Tratamento</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p>Gerencie tratamentos contínuos e complexos:</p>
                                <ul class="list-disc pl-5 space-y-2 mt-2">
                                    <li>Crie um <strong>Novo Tratamento</strong> definindo o tipo (ex: Ortodontia,
                                        Implante), valor total e número estimado de sessões.</li>
                                    <li><strong>Progresso</strong>: Acompanhe visualmente a evolução do tratamento
                                        através da barra de progresso.</li>
                                    <li><strong>Check-in de Sessão</strong>: Clique no botão <i
                                            data-lucide="check-circle" class="w-4 h-4 inline text-green-500"></i> na
                                        lista de tratamentos para marcar uma sessão como realizada.</li>
                                    <li>Quando todas as sessões forem completadas, o status muda automaticamente para
                                        <strong>Concluído</strong>.
                                    </li>
                                </ul>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="treatments" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>

                    <!-- 6. Financeiro e Estoque -->
                    <div id="manual-finance"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    6</div>
                                <h3 class="text-lg font-bold text-gray-800">Financeiro e Estoque</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p><strong>Financeiro:</strong></p>
                                <ul class="list-disc pl-5 mb-4 space-y-1">
                                    <li>Registre todas as entradas (consultas) e saídas (materiais, contas).</li>
                                    <li>Visualize o saldo do mês e categorias de gastos.</li>
                                </ul>
                                <p><strong>Estoque:</strong></p>
                                <ul class="list-disc pl-5 space-y-1">
                                    <li>Cadastre materiais (resina, luvas, anestésicos).</li>
                                    <li>Defina o estoque mínimo. O sistema avisará quando atingir o limite.</li>
                                    <li>Use os botões <strong>+1</strong> e <strong>-1</strong> para ajustes rápidos de
                                        quantidade.</li>
                                </ul>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="finance" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>

                    <!-- 7. Segurança e Dados -->
                    <div id="manual-backup"
                        class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
                        <div class="p-6 cursor-pointer hover:bg-gray-50 transition-colors flex items-center justify-between"
                            onclick="toggleManualSection(this)">
                            <div class="flex items-center gap-4">
                                <div
                                    class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center font-bold text-gray-500 group-hover:bg-plena-blue group-hover:text-white transition-colors">
                                    7</div>
                                <h3 class="text-lg font-bold text-gray-800">Backup e Segurança</h3>
                            </div>
                            <i data-lucide="chevron-down"
                                class="w-5 h-5 text-gray-400 transition-transform duration-300"></i>
                        </div>
                        <div class="px-6 pb-6 hidden transition-all duration-300 border-t border-gray-50 mt-2 pt-4">
                            <div class="prose prose-sm max-w-none text-gray-600 leading-relaxed">
                                <p class="mb-2">Seus dados ficam salvos <strong>apenas no navegador do seu
                                        dispositivo</strong>. O Plena Odonto não envia dados para a nuvem, garantindo
                                    total privacidade.</p>
                                <div class="bg-red-50 p-4 rounded-xl border border-red-200 text-red-800 mb-4">
                                    <strong>Importante:</strong> Se você limpar o cache do navegador ou perder o
                                    dispositivo, os dados serão perdidos se não houver backup.
                                </div>
                                <ul class="list-disc pl-5 space-y-2">
                                    <li><strong>Exportar Dados</strong>: Vá em Configurações > Backup e clique em
                                        "Exportar Dados". Isso baixará um arquivo <code>.json</code>.</li>
                                    <li>Salve este arquivo em um local seguro (Google Drive, pendrive, email)
                                        semanalmente.</li>
                                    <li><strong>Restaurar</strong>: Para recuperar, use a opção "Importar Dados" e
                                        selecione o arquivo de backup.</li>
                                </ul>
                            </div>
                            <label
                                class="flex items-center gap-3 mt-6 p-4 bg-green-50 rounded-xl cursor-pointer hover:bg-green-100 transition-colors border border-green-100">
                                <input type="checkbox"
                                    class="w-5 h-5 rounded text-plena-blue focus:ring-plena-blue form-checkbox manual-check"
                                    data-section="backup" onchange="updateManualProgress()">
                                <span class="font-bold text-green-800 text-sm">Marcar como lido</span>
                            </label>
                        </div>
                    </div>
                </div>

                <!-- Reset Progress -->
                <div class="mt-8 text-center">
                    <button onclick="resetManualProgress()"
                        class="text-sm text-gray-400 hover:text-red-500 underline transition-colors">
                        Reiniciar progresso do tutorial
                    </button>
                </div>
            </section>

            <!-- About View -->
            <section id="view-about" class="view-section hide fade-in">
                <div class="max-w-4xl mx-auto text-center pb-12">
                    <div
                        class="inline-flex items-center justify-center p-4 bg-gradient-to-r from-plena-blue to-plena-teal rounded-2xl mb-4 shadow-lg">
                        <i data-lucide="tooth" class="w-12 h-12 text-white"></i>
                    </div>
                    <h1 class="text-3xl font-bold text-gray-800 mb-2">Plena Odonto: A Solução Inteligente para sua Clínica</h1>
                    <p class="text-gray-600 text-lg max-w-2xl mx-auto">
                        Desenvolvido por especialistas para simplificar a gestão de clínicas e consultórios odontológicos, otimizar seu tempo e melhorar o atendimento ao paciente.
                    </p>

                    <div class="mt-8 text-left bg-white p-8 rounded-2xl shadow-sm border border-gray-100">
                        <h3 class="font-bold text-gray-800 mb-6 text-xl flex items-center">
                            <i data-lucide="star" class="w-5 h-5 mr-3 text-amber-500"></i>
                            Por que escolher a Plena Odonto?
                        </h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-x-8 gap-y-6">
                            <div class="flex items-start">
                                <i data-lucide="smartphone"
                                    class="w-8 h-8 text-plena-blue mr-4 flex-shrink-0 mt-1"></i>
                                <div>
                                    <h4 class="font-bold text-gray-800">100% Mobile e Offline</h4>
                                    <p class="text-sm text-gray-600">Acesse e gerencie de qualquer lugar, a qualquer
                                        hora, mesmo sem internet.</p>
                                </div>
                            </div>
                            <div class="flex items-start">
                                <i data-lucide="lock" class="w-8 h-8 text-plena-blue mr-4 flex-shrink-0 mt-1"></i>
                                <div>
                                    <h4 class="font-bold text-gray-800">Privacidade Total</h4>
                                    <p class="text-sm text-gray-600">Seus dados são armazenados exclusivamente no seu
                                        dispositivo. Ninguém mais tem acesso.</p>
                                </div>
                            </div>
                            <div class="flex items-start">
                                <i data-lucide="calendar-check"
                                    class="w-8 h-8 text-plena-blue mr-4 flex-shrink-0 mt-1"></i>
                                <div>
                                    <h4 class="font-bold text-gray-800">Agenda Inteligente</h4>
                                    <p class="text-sm text-gray-600">Visualize seus compromissos, agende novas consultas e evite conflitos de horário com facilidade.</p>
                                </div>
                            </div>
                            <div class="flex items-start">
                                <i data-lucide="zap" class="w-8 h-8 text-plena-blue mr-4 flex-shrink-0 mt-1"></i>
                                <div>
                                    <h4 class="font-bold text-gray-800">Automação que Facilita</h4>
                                    <p class="text-sm text-gray-600">Lembretes automáticos de consulta, controle de tratamentos e status de agendamento com um clique.</p>
                                </div>
                            </div>
                            <div class="flex items-start">
                                <i data-lucide="clipboard-list" class="w-8 h-8 text-plena-blue mr-4 flex-shrink-0 mt-1"></i>
                                <div>
                                    <h4 class="font-bold text-gray-800">Gestão Completa</h4>
                                    <p class="text-sm text-gray-600">Prontuário de pacientes, controle financeiro e de estoque de materiais, tudo em um só lugar.</p>
                                </div>
                            </div>
                            <div class="flex items-start">
                                <i data-lucide="file-text" class="w-8 h-8 text-plena-blue mr-4 flex-shrink-0 mt-1"></i>
                                <div>
                                    <h4 class="font-bold text-gray-800">Recibos e Orçamentos</h4>
                                    <p class="text-sm text-gray-600">Gere recibos de pagamento e orçamentos profissionais para seus pacientes em segundos.</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-8 text-left">
                        <h3 class="font-bold text-gray-800 mb-4 text-xl text-center">Nossa Missão</h3>
                        <p class="text-gray-600 max-w-3xl mx-auto text-center">
                            Nossa missão é empoderar dentistas e gestores de clínicas com uma ferramenta de gestão que antes era acessível apenas a grandes redes. Acreditamos que a tecnologia pode democratizar o sucesso na área da saúde, trazendo eficiência, segurança e mais tempo para você focar no que realmente importa: o sorriso dos seus pacientes.
                        </p>
                    </div>

                    <div class="mt-10">
                        <a href="https://plenaaplicativos.com.br" target="_blank"
                            class="inline-flex items-center gap-2 px-6 py-3 bg-plena-blue text-white rounded-lg text-sm font-bold hover:bg-plena-dark transition-all shadow-lg shadow-blue-200">
                            Conheça Nossas Outras Soluções
                            <i data-lucide="arrow-right" class="w-4 h-4"></i>
                        </a>
                    </div>
                </div>
            </section>
        </div>
    </main>

    <!-- MODALS -->
    <!-- Patient Modal -->
    <div id="patientModal" class="fixed inset-0 modal-backdrop hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-2xl transform transition-all p-6">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-xl font-bold text-gray-900" id="patient-modal-title">Novo Paciente</h3>
                <button onclick="closeModal('patientModal')"
                    class="bg-gray-100 hover:bg-gray-200 p-2 rounded-full text-gray-500 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>
            <form id="patient-form" onsubmit="submitPatient(event)" class="space-y-4">
                <input type="hidden" id="patient-id">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Nome Completo *</label>
                        <input type="text" id="patient-name" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">CPF</label>
                        <input type="text" id="patient-cpf"
                            class="mask-doc w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Data Nascimento</label>
                        <input type="date" id="patient-birth"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">WhatsApp *</label>
                        <input type="text" id="patient-phone" required
                            class="mask-phone w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div class="md:col-span-2">
                        <label class="text-xs font-bold text-gray-500 uppercase">Endereço</label>
                        <input type="text" id="patient-address"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div class="md:col-span-2">
                        <label class="text-xs font-bold text-gray-500 uppercase">Observações</label>
                        <textarea id="patient-notes" rows="3"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none"></textarea>
                    </div>
                </div>
                <div class="flex gap-2 pt-4">
                    <button type="button" onclick="deletePatient()" id="btn-delete-patient"
                        class="hidden px-4 py-3 text-red-500 hover:bg-red-50 rounded-xl font-bold border border-red-200">
                        Excluir Paciente
                    </button>
                    <button type="submit"
                        class="flex-1 bg-plena-blue text-white py-3 rounded-xl font-bold shadow-lg shadow-blue-200 hover:bg-plena-dark transition-all">
                        Salvar Paciente
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Appointment Modal -->
    <div id="appointmentModal" class="fixed inset-0 modal-backdrop hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-2xl transform transition-all p-6">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-xl font-bold text-gray-900" id="appointment-modal-title">Novo Agendamento</h3>
                <button onclick="closeModal('appointmentModal')"
                    class="bg-gray-100 hover:bg-gray-200 p-2 rounded-full text-gray-500 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>
            <form id="appointment-form" onsubmit="submitAppointment(event)" class="space-y-4">
                <input type="hidden" id="appointment-id">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Paciente *</label>
                        <select id="appointment-patient" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none bg-white">
                            <option value="">Selecione um paciente</option>
                        </select>
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Data *</label>
                        <input type="date" id="appointment-date" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Hora *</label>
                        <input type="time" id="appointment-time" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Duração (min)</label>
                        <input type="number" id="appointment-duration-input" value="30"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div class="md:col-span-2">
                        <label class="text-xs font-bold text-gray-500 uppercase">Procedimento *</label>
                        <select id="appointment-procedure" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none bg-white">
                            <option value="">Selecione o procedimento</option>
                            <option value="consulta">Consulta</option>
                            <option value="limpeza">Limpeza Dental</option>
                            <option value="restauracao">Restauração</option>
                            <option value="canal">Tratamento de Canal</option>
                            <option value="extracao">Extração</option>
                            <option value="ortodontia">Ortodontia</option>
                            <option value="protese">Prótese</option>
                            <option value="clareamento">Clareamento</option>
                        </select>
                    </div>
                    <div class="md:col-span-2">
                        <label class="text-xs font-bold text-gray-500 uppercase">Observações</label>
                        <textarea id="appointment-notes" rows="3"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none"></textarea>
                    </div>
                </div>
                <div class="flex gap-2 pt-4">
                    <button type="button" onclick="deleteAppointment()" id="btn-delete-appointment"
                        class="hidden px-4 py-3 text-red-500 hover:bg-red-50 rounded-xl font-bold border border-red-200">
                        Cancelar Agendamento
                    </button>
                    <button type="submit"
                        class="flex-1 bg-plena-blue text-white py-3 rounded-xl font-bold shadow-lg shadow-blue-200 hover:bg-plena-dark transition-all">
                        Salvar Agendamento
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Treatment Modal -->
    <div id="treatmentModal" class="fixed inset-0 modal-backdrop hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-2xl transform transition-all p-6">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-xl font-bold text-gray-900" id="treatment-modal-title">Novo Tratamento</h3>
                <button onclick="closeModal('treatmentModal')"
                    class="bg-gray-100 hover:bg-gray-200 p-2 rounded-full text-gray-500 transition-colors">
                    <i data-lucide="x" class="w-5 h-5"></i>
                </button>
            </div>
            <form id="treatment-form" onsubmit="submitTreatment(event)" class="space-y-4">
                <input type="hidden" id="treatment-id">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Paciente *</label>
                        <select id="treatment-patient" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none bg-white">
                            <option value="">Selecione um paciente</option>
                        </select>
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Tipo de Tratamento *</label>
                        <select id="treatment-type" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none bg-white">
                            <option value="">Selecione o tipo</option>
                            <option value="ortodontia">Ortodontia</option>
                            <option value="implante">Implante</option>
                            <option value="protese">Prótese</option>
                            <option value="clareamento">Clareamento</option>
                            <option value="periodontia">Periodontia</option>
                            <option value="endodontia">Endodontia</option>
                            <option value="cirurgia">Cirurgia</option>
                        </select>
                    </div>
                    <div class="md:col-span-2">
                        <label class="text-xs font-bold text-gray-500 uppercase">Descrição *</label>
                        <textarea id="treatment-description" rows="3" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none"></textarea>
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Valor Total *</label>
                        <input type="text" id="treatment-value" required
                            class="mask-currency w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Número de Sessões</label>
                        <input type="number" id="treatment-sessions" value="1"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                </div>
        </div>
        <div class="flex gap-2 pt-4">
            <button type="button" onclick="deleteTreatment()" id="btn-delete-treatment"
                class="hidden px-4 py-3 text-red-500 hover:bg-red-50 rounded-xl font-bold border border-red-200">
                Excluir Tratamento
            </button>
            <button type="submit"
                class="flex-1 bg-plena-blue text-white py-3 rounded-xl font-bold shadow-lg shadow-blue-200 hover:bg-plena-dark transition-all">
                Salvar Tratamento
            </button>
        </div>
        </form>
    </div>
    </div>

    <!-- Financial Modal -->
    <div id="financialModal" class="fixed inset-0 modal-backdrop hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-md transform transition-all p-6">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-lg font-bold text-gray-800">Nova Movimentação</h3>
                <button onclick="closeModal('financialModal')" class="text-gray-400 hover:text-gray-600">
                    <i data-lucide="x"></i>
                </button>
            </div>
            <form id="financial-form" onsubmit="submitFinancial(event)" class="space-y-4">
                <input type="hidden" id="financial-id">
                <div class="grid grid-cols-2 gap-3 p-1 bg-gray-100 rounded-lg">
                    <label class="cursor-pointer text-center">
                        <input type="radio" name="financial-type" value="income" class="peer hidden" checked>
                        <div
                            class="py-2 text-sm font-bold text-gray-500 rounded-md peer-checked:bg-white peer-checked:text-green-600 shadow-sm transition-all">
                            Receita
                        </div>
                    </label>
                    <label class="cursor-pointer text-center">
                        <input type="radio" name="financial-type" value="expense" class="peer hidden">
                        <div
                            class="py-2 text-sm font-bold text-gray-500 rounded-md peer-checked:bg-white peer-checked:text-red-600 shadow-sm transition-all">
                            Despesa
                        </div>
                    </label>
                </div>
                <div>
                    <label class="text-xs font-bold text-gray-500 uppercase">Valor (R$)</label>
                    <input type="text" id="financial-amount" required
                        class="mask-currency w-full border-2 border-gray-200 p-3 rounded-xl text-lg font-bold focus:border-plena-blue outline-none">
                </div>
                <div>
                    <label class="text-xs font-bold text-gray-500 uppercase">Descrição</label>
                    <input type="text" id="financial-description" required
                        class="w-full border p-2 rounded-lg text-sm focus:border-plena-blue outline-none">
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Categoria</label>
                        <select id="financial-category"
                            class="w-full border p-2 rounded-lg text-sm bg-white outline-none">
                            <option value="">Geral</option>
                            <option value="consulta">Consulta</option>
                            <option value="tratamento">Tratamento</option>
                            <option value="material">Material</option>
                            <option value="aluguel">Aluguel</option>
                            <option value="salario">Salário</option>
                            <option value="outros">Outros</option>
                        </select>
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Data</label>
                        <input type="date" id="financial-date" required
                            class="w-full border p-2 rounded-lg text-sm bg-white outline-none">
                    </div>
                </div>
                <div class="flex gap-2 pt-4">
                    <button type="button" onclick="deleteFinancial()" id="btn-delete-financial"
                        class="hidden px-4 py-3 text-red-500 hover:bg-red-50 rounded-xl font-bold border border-red-200">
                        Excluir
                    </button>
                    <button type="submit"
                        class="flex-1 bg-plena-blue text-white py-3 rounded-xl font-bold shadow-lg shadow-blue-200 hover:bg-plena-dark transition-all">
                        Salvar Movimentação
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Stock Modal -->
    <div id="stockModal" class="fixed inset-0 modal-backdrop hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl shadow-2xl w-full max-w-md transform transition-all p-6">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-lg font-bold text-gray-800" id="stock-modal-title">Novo Item no Estoque</h3>
                <button onclick="closeModal('stockModal')" class="text-gray-400 hover:text-gray-600">
                    <i data-lucide="x"></i>
                </button>
            </div>
            <form id="stock-form" onsubmit="submitStockItem(event)" class="space-y-4">
                <input type="hidden" id="stock-id">
                <div>
                    <label class="text-xs font-bold text-gray-500 uppercase">Nome do Item *</label>
                    <input type="text" id="stock-name" required
                        class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Quantidade *</label>
                        <input type="number" id="stock-quantity" required
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                    <div>
                        <label class="text-xs font-bold text-gray-500 uppercase">Mínimo Desejado</label>
                        <input type="number" id="stock-minimum"
                            class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none">
                    </div>
                </div>
                <div>
                    <label class="text-xs font-bold text-gray-500 uppercase">Categoria</label>
                    <select id="stock-category"
                        class="w-full border border-gray-300 p-3 rounded-xl text-sm mt-1 focus:ring-2 focus:ring-plena-blue outline-none bg-white">
                        <option value="medicamento">Medicamento</option>
                        <option value="material">Material</option>
                        <option value="equipamento">Equipamento</option>
                        <option value="consumivel">Consumível</option>
                    </select>
                </div>
                <div class="flex gap-2 pt-4">
                    <button type="button" onclick="deleteStockItem()" id="btn-delete-stock"
                        class="hidden px-4 py-3 text-red-500 hover:bg-red-50 rounded-xl font-bold border border-red-200">
                        Excluir
                    </button>
                    <button type="submit"
                        class="flex-1 bg-plena-blue text-white py-3 rounded-xl font-bold shadow-lg shadow-blue-200 hover:bg-plena-dark transition-all">
                        Salvar Item
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        // 🔒 DADOS DA LICENÇA
        const LICENSE_DATA = {
            active: false,
            ownerName: "",
            ownerDoc: ""
        };

        // ESTADO GLOBAL
        const DB_KEY = 'plena_odonto_v1';
        const defaultDB = {
            patients: [],
            appointments: [],
            treatments: [],
            financial: [],
            stock: [],
            settings: {
                clinicName: 'Minha Clínica Odontológica',
                dentistCRO: '',
                clinicPhone: '',
                appointmentDuration: 30,
                reminderHours: 24,
                ownerName: '',
                ownerDoc: ''
            }
        };

        let db = JSON.parse(localStorage.getItem(DB_KEY)) || defaultDB;

        // UTILITÁRIOS
        const save = () => {
            localStorage.setItem(DB_KEY, JSON.stringify(db));
        };

        const fmtMoney = (v) => {
            return v.toLocaleString('pt-BR', {
                style: 'currency',
                currency: 'BRL',
                minimumFractionDigits: 2,
                maximumFractionDigits: 2
            });
        };

        const fmtDate = (d) => {
            const date = new Date(d);
            return date.toLocaleDateString('pt-BR');
        };

        const fmtDateTime = (d, t) => {
            const date = new Date(d + 'T' + t);
            return date.toLocaleString('pt-BR', {
                day: '2-digit',
                month: '2-digit',
                hour: '2-digit',
                minute: '2-digit'
            });
        };

        const getID = () => {
            return Date.now().toString(36) + Math.random().toString(36).substr(2, 5);
        };

        const sanitizeHTML = (str) => {
            if (str === null || typeof str === 'undefined') return '';
            const map = {
                '&': '&amp;',
                '<': '&lt;',
                '>': '&gt;',
                '"': '&quot;',
                "'": '&#039;'
            };
            return str.toString().replace(/[&<>"']/g, (m) => map[m]);
        };

        const maskCurrency = (value) => {
            if (!value) return '';
            value = value.replace(/\D/g, "");
            value = (Number(value) / 100).toFixed(2) + "";
            value = value.replace(".", ",");
            value = value.replace(/(\d)(?=(\d{3})+(?!\d))/g, "$1.");
            return "R$ " + value;
        };

        const parseCurrency = (value) => {
            if (!value) return 0;
            if (typeof value === 'number') return value;
            return parseFloat(value.replace(/[^0-9,-]+/g, "").replace(",", ".")) || 0;
        };

        const maskPhone = (v) => {
            if (!v) return '';
            v = v.replace(/\D/g, "");
            v = v.replace(/^(\d{2})(\d)/g, "($1) $2");
            v = v.replace(/(\d)(\d{4})$/, "$1-$2");
            return v;
        };

        const maskCPF = (v) => {
            if (!v) return '';
            v = v.replace(/\D/g, "");
            v = v.replace(/(\d{3})(\d)/, "$1.$2");
            v = v.replace(/(\d{3})(\d)/, "$1.$2");
            v = v.replace(/(\d{3})(\d{1,2})$/, "$1-$2");
            return v;
        };

        const setupInputMasks = () => {
            document.querySelectorAll('.mask-currency').forEach(input => {
                input.addEventListener('input', (e) => {
                    e.target.value = maskCurrency(e.target.value);
                });
            });
            document.querySelectorAll('.mask-phone').forEach(input => {
                input.addEventListener('input', (e) => {
                    e.target.maxLength = 15;
                    e.target.value = maskPhone(e.target.value);
                });
            });
            document.querySelectorAll('.mask-doc').forEach(input => {
                input.addEventListener('input', (e) => {
                    e.target.maxLength = 18;
                    e.target.value = maskCPF(e.target.value);
                });
            });
        };

        // INICIALIZAÇÃO
        function init() {
            try {
                if (window.lucide) lucide.createIcons();
                setupInputMasks();

                // Validation of DB integrity
                if (!db.patients) db.patients = [];
                if (!db.appointments) db.appointments = [];
                if (!db.treatments) db.treatments = [];
                if (!db.financial) db.financial = [];
                if (!db.stock) db.stock = [];
                if (!db.settings) db.settings = defaultDB.settings;

                // Configurar datas padrão
                const today = new Date().toISOString().split('T')[0];
                document.getElementById('schedule-date').value = today;
                document.getElementById('appointment-date').value = today;
                document.getElementById('financial-date').value = today;

                // Aplicar licença se ativa
                if (typeof LICENSE_DATA !== 'undefined' && LICENSE_DATA.active) {
                    db.settings.ownerName = LICENSE_DATA.ownerName;
                    db.settings.ownerDoc = LICENSE_DATA.ownerDoc;
                    save();
                }

                // Carregar configurações
                if (db.settings.clinicName) document.getElementById('clinic-name').value = db.settings.clinicName;
                if (db.settings.dentistCRO) document.getElementById('dentist-cro').value = db.settings.dentistCRO;
                if (db.settings.clinicPhone) document.getElementById('clinic-phone').value = db.settings.clinicPhone;
                if (db.settings.appointmentDuration) document.getElementById('appointment-duration').value = db.settings.appointmentDuration;
                if (db.settings.reminderHours) document.getElementById('reminder-hours').value = db.settings.reminderHours;

                // Renderizar dashboard inicial
                renderDashboard();
                setInterval(save, 30000);
            } catch (error) {
                console.error('Erro na inicialização:', error);
                alert('Ocorreu um erro ao iniciar o aplicativo: ' + error.message);
            }
        }

        // ROTEAMENTO
        function router(view) {
            document.querySelectorAll('.view-section').forEach(el => el.classList.add('hide'));
            document.querySelectorAll('.nav-item').forEach(el => {
                el.classList.remove('bg-white/10', 'text-white');
                el.classList.add('text-gray-400');
            });

            const viewElement = document.getElementById(`view-${view}`);
            if (viewElement) {
                viewElement.classList.remove('hide');
                viewElement.classList.add('fade-in');
            }

            const navElement = document.getElementById(`nav-${view}`);
            if (navElement) {
                navElement.classList.add('bg-white/10', 'text-white');
                navElement.classList.remove('text-gray-400');
            }

            const titles = {
                dashboard: 'Dashboard',
                schedule: 'Agenda',
                patients: 'Pacientes',
                treatments: 'Tratamentos',
                financial: 'Financeiro',
                stock: 'Estoque',
                reports: 'Relatórios',
                settings: 'Configurações',
                manual: 'Manual de Uso',
                about: 'Sobre a Plena'
            };

            document.getElementById('page-title').innerText = titles[view] || 'Plena Odonto';

            if (window.innerWidth < 1024) {
                toggleSidebar();
            }

            // Renderizar dados específicos
            if (view === 'dashboard') renderDashboard();
            else if (view === 'patients') renderPatients();
            else if (view === 'schedule') renderSchedule();
            else if (view === 'treatments') renderTreatments();
            else if (view === 'financial') renderFinancial();
            else if (view === 'stock') renderStock();
            else if (view === 'manual') initManual();
            else if (view === 'about') {
                // Nenhuma renderização específica necessária para a página estática "Sobre"
            }
        }

        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('overlay');
            sidebar.classList.toggle('open');
            overlay.classList.toggle('hidden');
            document.body.style.overflow = sidebar.classList.contains('open') ? 'hidden' : '';
        }

        // DASHBOARD
        function renderDashboard() {
            const today = new Date().toISOString().split('T')[0];
            const todayAppointments = db.appointments.filter(a => a.date === today);
            const activeTreatments = db.treatments.filter(t => t.status === 'active');
            const nextWeekAppointments = db.appointments.filter(a => {
                const appDate = new Date(a.date);
                const today = new Date();
                const nextWeek = new Date();
                nextWeek.setDate(today.getDate() + 7);
                return appDate >= today && appDate <= nextWeek;
            });

            // Calcular receita do mês
            const currentMonth = new Date().toISOString().slice(0, 7);
            const monthFinancial = db.financial.filter(f => f.date.startsWith(currentMonth) && f.type === 'income');
            const monthRevenue = monthFinancial.reduce((sum, f) => sum + f.amount, 0);

            // Atualizar stats
            document.getElementById('dash-today').innerText = todayAppointments.length;
            document.getElementById('dash-patients').innerText = db.patients.length;
            document.getElementById('dash-revenue').innerText = fmtMoney(monthRevenue);
            document.getElementById('dash-next').innerText = nextWeekAppointments.length;

            // Renderizar agenda de hoje
            const scheduleContainer = document.getElementById('today-schedule');
            if (todayAppointments.length === 0) {
                scheduleContainer.innerHTML = `
                    <div class="text-center py-8 text-gray-400">
                        <i data-lucide="calendar" class="w-12 h-12 mx-auto mb-3"></i>
                        <p>Nenhuma consulta agendada para hoje</p>
                    </div>
                `;
            } else {
                scheduleContainer.innerHTML = todayAppointments.map(app => `
                    <div class="flex items-center justify-between p-3 border border-gray-100 rounded-lg hover:bg-gray-50">
                        <div class="flex items-center gap-3">
                            <div class="status-indicator status-${app.status}"></div>
                            <div>
                                <p class="font-medium text-gray-800">${app.time} - ${app.procedure}</p>
                                <p class="text-sm text-gray-500">${getPatientName(app.patientId)}</p>
                            </div>
                        </div>
                        <button onclick="changeAppointmentStatus('${app.id}')" class="text-xs px-3 py-1 rounded-full ${getStatusColor(app.status)}">
                            ${getStatusText(app.status)}
                        </button>
                    </div>
                `).join('');
            }

            // Renderizar progresso de tratamentos
            const progressContainer = document.getElementById('treatment-progress');
            if (activeTreatments.length === 0) {
                progressContainer.innerHTML = '<p class="text-gray-400 text-sm">Nenhum tratamento em andamento</p>';
            } else {
                progressContainer.innerHTML = activeTreatments.slice(0, 3).map(t => `
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm font-medium text-gray-800 truncate">${getPatientName(t.patientId)}</p>
                            <p class="text-xs text-gray-500">${t.type}</p>
                        </div>
                        <div class="text-right">
                            <p class="text-xs font-bold text-plena-blue">${fmtMoney(t.value)}</p>
                            <div class="progress-bar w-20">
                                <div class="progress-fill" style="width: ${calculateProgress(t)}%"></div>
                            </div>
                        </div>
                    </div>
                `).join('');
            }

            if (window.lucide) lucide.createIcons();
        }

        function getPatientName(id) {
            const patient = db.patients.find(p => p.id === id);
            return patient ? sanitizeHTML(patient.name) : 'Paciente não encontrado';
        }

        function getStatusColor(status) {
            const colors = {
                'scheduled': 'bg-blue-100 text-blue-700',
                'confirmed': 'bg-green-100 text-green-700',
                'in_progress': 'bg-amber-100 text-amber-700',
                'completed': 'bg-purple-100 text-purple-700',
                'cancelled': 'bg-red-100 text-red-700',
                'no_show': 'bg-gray-100 text-gray-700'
            };
            return colors[status] || 'bg-gray-100 text-gray-700';
        }

        function getStatusText(status) {
            const texts = {
                'scheduled': 'Agendado',
                'confirmed': 'Confirmado',
                'in_progress': 'Em atendimento',
                'completed': 'Concluído',
                'cancelled': 'Cancelado',
                'no_show': 'Falta'
            };
            return texts[status] || 'Agendado';
        }

        function calculateProgress(treatment) {
            return Math.min(Math.floor((treatment.completedSessions || 0) / (treatment.totalSessions || 1) * 100), 100);
        }

        // MODAL FUNCTIONS
        function openModal(id) {
            const modal = document.getElementById(id);
            if (modal) {
                modal.classList.remove('hidden');
                document.body.style.overflow = 'hidden';
            }
        }

        function closeModal(id) {
            const modal = document.getElementById(id);
            if (modal) {
                modal.classList.add('hidden');
                document.body.style.overflow = '';
            }
        }

        // PACIENTES
        function openPatientModal(id = null) {
            if (id) {
                const patient = db.patients.find(p => p.id === id);
                if (patient) {
                    document.getElementById('patient-modal-title').textContent = 'Editar Paciente';
                    document.getElementById('patient-id').value = patient.id;
                    document.getElementById('patient-name').value = patient.name;
                    document.getElementById('patient-cpf').value = patient.cpf || '';
                    document.getElementById('patient-birth').value = patient.birth || '';
                    document.getElementById('patient-phone').value = patient.phone || '';
                    document.getElementById('patient-address').value = patient.address || '';
                    document.getElementById('patient-notes').value = patient.notes || '';
                    document.getElementById('btn-delete-patient').classList.remove('hidden');
                }
            } else {
                document.getElementById('patient-modal-title').textContent = 'Novo Paciente';
                document.getElementById('patient-form').reset();
                document.getElementById('patient-id').value = '';
                document.getElementById('btn-delete-patient').classList.add('hidden');
            }
            openModal('patientModal');
        }

        function submitPatient(e) {
            e.preventDefault();
            try {
                const id = document.getElementById('patient-id').value;
                const nameHelper = document.getElementById('patient-name');
                if (!nameHelper || !nameHelper.value) throw new Error('Nome do paciente é obrigatório');

                let patient = {
                    id: id || getID(),
                    name: nameHelper.value,
                    cpf: document.getElementById('patient-cpf').value,
                    birth: document.getElementById('patient-birth').value,
                    phone: document.getElementById('patient-phone').value,
                    address: document.getElementById('patient-address').value,
                    notes: document.getElementById('patient-notes').value,
                    createdAt: new Date().toISOString()
                };

                if (id) {
                    const index = db.patients.findIndex(p => p.id === id);
                    if (index !== -1) {
                        patient.createdAt = db.patients[index].createdAt;
                        db.patients[index] = patient;
                    }
                } else {
                    db.patients.push(patient);
                }

                save();
                closeModal('patientModal');
                renderPatients();
                if (document.getElementById('view-dashboard') && !document.getElementById('view-dashboard').classList.contains('hide')) {
                    renderDashboard();
                }
                showNotification('Paciente salvo com sucesso!', 'success');
            } catch (error) {
                console.error(error);
                showNotification('Erro ao salvar paciente: ' + error.message, 'error');
            }
        }

        function deletePatient() {
            const id = document.getElementById('patient-id').value;
            if (confirm('Tem certeza que deseja excluir este paciente? Todos os tratamentos e agendamentos relacionados serão perdidos.')) {
                db.patients = db.patients.filter(p => p.id !== id);
                db.appointments = db.appointments.filter(a => a.patientId !== id);
                db.treatments = db.treatments.filter(t => t.patientId !== id);
                save();
                closeModal('patientModal');
                renderPatients();
                renderDashboard();
                showNotification('Paciente excluído com sucesso!', 'success');
            }
        }

        function renderPatients() {
            const container = document.getElementById('patients-list');
            const search = document.getElementById('search-patients').value.toLowerCase();

            let patients = db.patients;
            if (search) {
                patients = patients.filter(p =>
                    p.name.toLowerCase().includes(search) ||
                    (p.cpf && p.cpf.includes(search)) ||
                    (p.phone && p.phone.includes(search))
                );
            }

            if (patients.length === 0) {
                container.innerHTML = `
                    <div class="col-span-3 bg-white p-8 rounded-2xl text-center border border-gray-100">
                        <i data-lucide="users" class="w-12 h-12 mx-auto mb-4 text-gray-300"></i>
                        <h3 class="text-lg font-bold text-gray-800 mb-2">Nenhum paciente cadastrado</h3>
                        <p class="text-gray-500 mb-4">Comece cadastrando seu primeiro paciente</p>
                        <button onclick="openPatientModal()" class="bg-plena-blue text-white px-4 py-2 rounded-lg font-bold hover:bg-plena-dark">
                            Cadastrar Primeiro Paciente
                        </button>
                    </div>
                `;
                return;
            }

            container.innerHTML = patients.map(p => `
                <div class="bg-white p-6 rounded-2xl border border-gray-100 shadow-sm card-hover cursor-pointer" onclick="openPatientModal('${p.id}')">
                    <div class="flex items-center gap-3 mb-4">
                        <div class="w-12 h-12 rounded-full bg-plena-blue/10 flex items-center justify-center">
                            <i data-lucide="user" class="w-6 h-6 text-plena-blue"></i>
                        </div>
                        <div class="flex-1">
                            <h3 class="font-bold text-gray-800 truncate">${sanitizeHTML(p.name)}</h3>
                            <p class="text-xs text-gray-500">${p.phone || 'Sem telefone'}</p>
                        </div>
                    </div>
                    <div class="space-y-2 text-sm">
                        ${p.cpf ? `<div class="flex items-center gap-2"><i data-lucide="id-card" class="w-3 h-3 text-gray-400"></i><span>${p.cpf}</span></div>` : ''}
                        ${p.birth ? `<div class="flex items-center gap-2"><i data-lucide="calendar" class="w-3 h-3 text-gray-400"></i><span>${fmtDate(p.birth)}</span></div>` : ''}
                    </div>
                    <div class="mt-4 pt-4 border-t border-gray-100 flex justify-between">
                        <span class="text-xs text-gray-500">Cadastrado em ${fmtDate(p.createdAt)}</span>
                        <button onclick="event.stopPropagation(); openAppointmentModal(null, '${p.id}')" 
                                class="text-xs bg-plena-blue text-white px-3 py-1 rounded hover:bg-plena-dark">
                            Agendar
                        </button>
                    </div>
                </div>
            `).join('');
        }

        // AGENDAMENTOS
        function openAppointmentModal(id = null, patientId = null) {
            // Popular select de pacientes
            const patientSelect = document.getElementById('appointment-patient');
            patientSelect.innerHTML = '<option value="">Selecione um paciente</option>' +
                db.patients.map(p => `<option value="${p.id}">${p.name}</option>`).join('');

            if (id) {
                const appointment = db.appointments.find(a => a.id === id);
                if (appointment) {
                    document.getElementById('appointment-modal-title').textContent = 'Editar Agendamento';
                    document.getElementById('appointment-id').value = appointment.id;
                    document.getElementById('appointment-patient').value = appointment.patientId;
                    document.getElementById('appointment-date').value = appointment.date;
                    document.getElementById('appointment-time').value = appointment.time;
                    document.getElementById('appointment-duration-input').value = appointment.duration || 30;
                    document.getElementById('appointment-procedure').value = appointment.procedure;
                    document.getElementById('appointment-notes').value = appointment.notes || '';
                    document.getElementById('btn-delete-appointment').classList.remove('hidden');
                }
            } else {
                document.getElementById('appointment-modal-title').textContent = 'Novo Agendamento';
                document.getElementById('appointment-form').reset();
                document.getElementById('appointment-id').value = '';
                document.getElementById('appointment-duration-input').value = db.settings.appointmentDuration || 30;
                if (patientId) document.getElementById('appointment-patient').value = patientId;
                document.getElementById('btn-delete-appointment').classList.add('hidden');
            }
            openModal('appointmentModal');
        }

        function submitAppointment(e) {
            e.preventDefault();
            try {
                const id = document.getElementById('appointment-id').value;
                const patientSelect = document.getElementById('appointment-patient');
                const dateInput = document.getElementById('appointment-date');
                const timeInput = document.getElementById('appointment-time');

                if (!patientSelect.value) throw new Error('Selecione um paciente');
                if (!dateInput.value) throw new Error('Data é obrigatória');
                if (!timeInput.value) throw new Error('Hora é obrigatória');

                let appointment = {
                    id: id || getID(),
                    patientId: patientSelect.value,
                    date: dateInput.value,
                    time: timeInput.value,
                    duration: parseInt(document.getElementById('appointment-duration-input').value) || 30,
                    procedure: document.getElementById('appointment-procedure').value,
                    notes: document.getElementById('appointment-notes').value,
                    status: 'scheduled',
                    createdAt: new Date().toISOString()
                };

                if (id) {
                    const index = db.appointments.findIndex(a => a.id === id);
                    if (index !== -1) {
                        appointment.createdAt = db.appointments[index].createdAt;
                        db.appointments[index] = appointment;
                    }
                } else {
                    db.appointments.push(appointment);
                }

                save();
                closeModal('appointmentModal');
                renderSchedule();
                if (document.getElementById('view-dashboard') && !document.getElementById('view-dashboard').classList.contains('hide')) {
                    renderDashboard();
                }
                showNotification('Agendamento salvo com sucesso!', 'success');
            } catch (error) {
                console.error(error);
                showNotification('Erro ao salvar agendamento: ' + error.message, 'error');
            }
        }

        function deleteAppointment() {
            const id = document.getElementById('appointment-id').value;
            if (confirm('Tem certeza que deseja excluir ou cancelar este agendamento?')) {
                db.appointments = db.appointments.filter(a => a.id !== id);
                save();
                closeModal('appointmentModal');
                renderSchedule();
                renderDashboard();
                showNotification('Agendamento excluído com sucesso!', 'success');
            }
        }

        function renderSchedule() {
            const date = document.getElementById('schedule-date').value;
            const appointments = db.appointments.filter(a => a.date === date);
            const container = document.getElementById('schedule-timeline');

            if (appointments.length === 0) {
                container.innerHTML = `
                    <div class="text-center py-12 text-gray-400">
                        <i data-lucide="calendar" class="w-16 h-16 mx-auto mb-4"></i>
                        <h3 class="text-lg font-bold text-gray-800 mb-2">Nenhum agendamento</h3>
                        <p class="text-gray-500">Não há consultas agendadas para esta data</p>
                    </div>
                `;
                return;
            }

            // Ordenar por hora
            appointments.sort((a, b) => a.time.localeCompare(b.time));

            container.innerHTML = appointments.map(app => `
                <div class="timeline-item">
                    <div class="bg-white p-4 rounded-xl border border-gray-100 shadow-sm">
                        <div class="flex justify-between items-start">
                            <div>
                                <div class="flex items-center gap-2 mb-1">
                                    <span class="font-bold text-gray-800">${app.time}</span>
                                    <span class="text-xs px-2 py-0.5 rounded-full ${getStatusColor(app.status)}">
                                        ${getStatusText(app.status)}
                                    </span>
                                </div>
                                <h4 class="font-bold text-gray-800">${getPatientName(app.patientId)}</h4>
                                <p class="text-sm text-gray-600">${app.procedure}</p>
                                ${app.notes ? `<p class="text-xs text-gray-500 mt-2">${sanitizeHTML(app.notes)}</p>` : ''}
                            </div>
                            <div class="flex gap-2">
                                <button onclick="changeAppointmentStatus('${app.id}')" 
                                        class="p-2 text-gray-500 hover:bg-gray-100 rounded-lg">
                                    <i data-lucide="check-circle"></i>
                                </button>
                                <button onclick="openAppointmentModal('${app.id}')" 
                                        class="p-2 text-blue-500 hover:bg-blue-50 rounded-lg">
                                    <i data-lucide="edit-2"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            `).join('');

            if (window.lucide) lucide.createIcons();
        }

        function changeAppointmentStatus(id) {
            const appointment = db.appointments.find(a => a.id === id);
            if (!appointment) return;

            const statuses = ['scheduled', 'confirmed', 'in_progress', 'completed', 'cancelled', 'no_show'];
            const currentIndex = statuses.indexOf(appointment.status);
            const nextIndex = (currentIndex + 1) % statuses.length;
            appointment.status = statuses[nextIndex];

            save();
            renderSchedule();
            if (document.getElementById('view-dashboard').classList.contains('hide') === false) {
                renderDashboard();
            }

            showNotification(`Status alterado para: ${getStatusText(appointment.status)}`, 'success');
        }

        // TRATAMENTOS
        function openTreatmentModal(id = null) {
            // Popular select de pacientes
            const patientSelect = document.getElementById('treatment-patient');
            patientSelect.innerHTML = '<option value="">Selecione um paciente</option>' +
                db.patients.map(p => `<option value="${p.id}">${p.name}</option>`).join('');

            if (id) {
                const treatment = db.treatments.find(t => t.id === id);
                if (treatment) {
                    document.getElementById('treatment-modal-title').textContent = 'Editar Tratamento';
                    document.getElementById('treatment-id').value = treatment.id;
                    document.getElementById('treatment-patient').value = treatment.patientId;
                    document.getElementById('treatment-type').value = treatment.type;
                    document.getElementById('treatment-description').value = treatment.description;
                    document.getElementById('treatment-value').value = fmtMoney(treatment.value);
                    document.getElementById('treatment-sessions').value = treatment.totalSessions || 1;
                    document.getElementById('btn-delete-treatment').classList.remove('hidden');
                }
            } else {
                document.getElementById('treatment-modal-title').textContent = 'Novo Tratamento';
                document.getElementById('treatment-form').reset();
                document.getElementById('treatment-id').value = '';
                document.getElementById('btn-delete-treatment').classList.add('hidden');
            }
            openModal('treatmentModal');
        }

        function submitTreatment(e) {
            e.preventDefault();
            try {
                const id = document.getElementById('treatment-id').value;
                const patientSelect = document.getElementById('treatment-patient');
                if (!patientSelect.value) throw new Error('Selecione um paciente');

                let treatment = {
                    id: id || getID(),
                    patientId: patientSelect.value,
                    type: document.getElementById('treatment-type').value,
                    description: document.getElementById('treatment-description').value,
                    value: parseCurrency(document.getElementById('treatment-value').value),
                    totalSessions: parseInt(document.getElementById('treatment-sessions').value) || 1,
                    completedSessions: 0,
                    status: 'active',
                    createdAt: new Date().toISOString()
                };

                if (id) {
                    const index = db.treatments.findIndex(t => t.id === id);
                    if (index !== -1) {
                        treatment.createdAt = db.treatments[index].createdAt;
                        treatment.status = db.treatments[index].status;
                        treatment.completedSessions = db.treatments[index].completedSessions;
                        db.treatments[index] = treatment;
                    }
                } else {
                    db.treatments.push(treatment);
                }

                save();
                closeModal('treatmentModal');
                renderTreatments();
                if (document.getElementById('view-dashboard') && !document.getElementById('view-dashboard').classList.contains('hide')) {
                    renderDashboard();
                }
                showNotification('Tratamento salvo com sucesso!', 'success');
            } catch (error) {
                console.error(error);
                showNotification('Erro ao salvar tratamento: ' + error.message, 'error');
            }
        }

        function deleteTreatment() {
            const id = document.getElementById('treatment-id').value;
            if (confirm('Tem certeza que deseja excluir este tratamento?')) {
                db.treatments = db.treatments.filter(t => t.id !== id);
                save();
                closeModal('treatmentModal');
                renderTreatments();
                renderDashboard();
                showNotification('Tratamento excluído com sucesso!', 'success');
            }
        }

        function renderTreatments() {
            const tbody = document.getElementById('treatments-table-body');

            if (db.treatments.length === 0) {
                tbody.innerHTML = `
                    <tr>
                        <td colspan="6" class="py-12 text-center">
                            <div class="flex flex-col items-center justify-center">
                                <div class="bg-gray-50 p-4 rounded-full mb-4">
                                    <i data-lucide="activity" class="w-12 h-12 text-gray-300"></i>
                                </div>
                                <h3 class="text-gray-900 font-bold text-lg mb-1">Nenhum tratamento cadastrado</h3>
                                <p class="text-gray-500 text-sm">Comece criando seu primeiro plano de tratamento</p>
                            </div>
                        </td>
                    </tr>
                `;
                return;
            }

            tbody.innerHTML = db.treatments.map(t => `
                <tr class="hover:bg-gray-50">
                    <td class="px-6 py-4">${getPatientName(t.patientId)}</td>
                    <td class="px-6 py-4">
                        <div class="font-bold text-gray-800">${t.type}</div>
                        <div class="text-xs text-gray-500 truncate max-w-xs">${t.description}</div>
                    </td>
                    <td class="px-6 py-4">
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold ${t.status === 'active' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-700'}">
                            ${t.status === 'active' ? 'Ativo' : 'Concluído'}
                        </span>
                    </td>
                    <td class="px-6 py-4 font-bold text-plena-blue">${fmtMoney(t.value)}</td>
                    <td class="px-6 py-4">
                        <div class="flex items-center gap-2">
                            <div class="progress-bar flex-1">
                                <div class="progress-fill" style="width: ${calculateProgress(t)}%"></div>
                            </div>
                            <span class="text-xs text-gray-500">${t.completedSessions || 0}/${t.totalSessions}</span>
                        </div>
                    </td>
                    <td class="px-6 py-4 text-center">
                        <div class="flex justify-center gap-2">
                            <button onclick="openTreatmentModal('${t.id}')" 
                                    class="p-2 text-blue-500 hover:bg-blue-50 rounded-lg">
                                <i data-lucide="edit-2" class="w-4 h-4"></i>
                            </button>
                            <button onclick="completeSession('${t.id}')" 
                                    class="p-2 text-green-500 hover:bg-green-50 rounded-lg">
                                <i data-lucide="check-circle" class="w-4 h-4"></i>
                            </button>
                        </div>
                    </td>
                </tr>
            `).join('');

            if (window.lucide) lucide.createIcons();
        }

        function completeSession(treatmentId) {
            const treatment = db.treatments.find(t => t.id === treatmentId);
            if (!treatment) return;

            treatment.completedSessions = (treatment.completedSessions || 0) + 1;
            if (treatment.completedSessions >= treatment.totalSessions) {
                treatment.status = 'completed';
            }

            save();
            renderTreatments();
            if (document.getElementById('view-dashboard').classList.contains('hide') === false) {
                renderDashboard();
            }
            showNotification('Sessão registrada com sucesso!', 'success');
        }

        // FINANCEIRO
        function openFinancialModal(id = null) {
            if (id) {
                const financial = db.financial.find(f => f.id === id);
                if (financial) {
                    document.getElementById('financial-id').value = financial.id;
                    document.querySelector(`input[name="financial-type"][value="${financial.type}"]`).checked = true;
                    document.getElementById('financial-amount').value = fmtMoney(financial.amount);
                    document.getElementById('financial-description').value = financial.description;
                    document.getElementById('financial-category').value = financial.category || '';
                    document.getElementById('financial-date').value = financial.date;
                    document.getElementById('btn-delete-financial').classList.remove('hidden');
                }
            } else {
                document.getElementById('financial-form').reset();
                document.getElementById('financial-id').value = '';
                document.getElementById('financial-date').value = new Date().toISOString().split('T')[0];
                document.getElementById('btn-delete-financial').classList.add('hidden');
            }
            openModal('financialModal');
        }

        function submitFinancial(e) {
            e.preventDefault();
            try {
                const id = document.getElementById('financial-id').value;
                const typeInput = document.querySelector('input[name="financial-type"]:checked');
                if (!typeInput) throw new Error('Selecione o tipo de movimentação');

                let financial = {
                    id: id || getID(),
                    type: typeInput.value,
                    amount: parseCurrency(document.getElementById('financial-amount').value),
                    description: document.getElementById('financial-description').value,
                    category: document.getElementById('financial-category').value,
                    date: document.getElementById('financial-date').value,
                    createdAt: new Date().toISOString()
                };

                if (id) {
                    const index = db.financial.findIndex(f => f.id === id);
                    if (index !== -1) {
                        financial.createdAt = db.financial[index].createdAt;
                        db.financial[index] = financial;
                    }
                } else {
                    db.financial.push(financial);
                }

                save();
                closeModal('financialModal');
                renderFinancial();
                if (document.getElementById('view-dashboard') && !document.getElementById('view-dashboard').classList.contains('hide')) {
                    renderDashboard();
                }
                showNotification('Movimentação salva com sucesso!', 'success');
            } catch (error) {
                console.error(error);
                showNotification('Erro ao salvar movimentação: ' + error.message, 'error');
            }
        }

        function deleteFinancial() {
            const id = document.getElementById('financial-id').value;
            if (confirm('Tem certeza que deseja excluir esta movimentação?')) {
                db.financial = db.financial.filter(f => f.id !== id);
                save();
                closeModal('financialModal');
                renderFinancial();
                renderDashboard();
                showNotification('Movimentação excluída com sucesso!', 'success');
            }
        }

        function renderFinancial() {
            const tbody = document.getElementById('finance-table-body');
            const currentMonth = new Date().toISOString().slice(0, 7);

            const monthIncome = db.financial.filter(f => f.date.startsWith(currentMonth) && f.type === 'income')
                .reduce((sum, f) => sum + f.amount, 0);
            const monthExpense = db.financial.filter(f => f.date.startsWith(currentMonth) && f.type === 'expense')
                .reduce((sum, f) => sum + f.amount, 0);
            const totalBalance = monthIncome - monthExpense;

            document.getElementById('finance-income').innerText = fmtMoney(monthIncome);
            document.getElementById('finance-expense').innerText = fmtMoney(monthExpense);
            document.getElementById('finance-balance').innerText = fmtMoney(totalBalance);

            if (db.financial.length === 0) {
                tbody.innerHTML = `
                    <tr>
                        <td colspan="6" class="py-12 text-center">
                            <div class="flex flex-col items-center justify-center">
                                <div class="bg-gray-50 p-4 rounded-full mb-4">
                                    <i data-lucide="wallet" class="w-12 h-12 text-gray-300"></i>
                                </div>
                                <h3 class="text-gray-900 font-bold text-lg mb-1">Nenhuma movimentação</h3>
                                <p class="text-gray-500 text-sm">Registre sua primeira receita ou despesa</p>
                            </div>
                        </td>
                    </tr>
                `;
                return;
            }

            // Ordenar por data (mais recente primeiro)
            const sortedFinancial = [...db.financial].sort((a, b) => new Date(b.date) - new Date(a.date));

            tbody.innerHTML = sortedFinancial.map(f => `
                <tr class="hover:bg-gray-50">
                    <td class="px-6 py-4 text-sm text-gray-500">${fmtDate(f.date)}</td>
                    <td class="px-6 py-4">
                        <div class="font-bold text-gray-800">${f.description}</div>
                        <div class="text-xs text-gray-500">${f.category || 'Geral'}</div>
                    </td>
                    <td class="px-6 py-4 text-sm text-gray-500">-</td>
                    <td class="px-6 py-4">
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold ${f.type === 'income' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700'}">
                            ${f.type === 'income' ? 'Receita' : 'Despesa'}
                        </span>
                    </td>
                    <td class="px-6 py-4 text-right font-bold ${f.type === 'income' ? 'text-green-600' : 'text-red-500'}">
                        ${f.type === 'income' ? '+' : '-'} ${fmtMoney(f.amount)}
                    </td>
                    <td class="px-6 py-4 text-center">
                        <button onclick="openFinancialModal('${f.id}')" 
                                class="p-2 text-blue-500 hover:bg-blue-50 rounded-lg">
                            <i data-lucide="edit-2" class="w-4 h-4"></i>
                        </button>
                    </td>
                </tr>
            `).join('');

            if (window.lucide) lucide.createIcons();
        }

        // ESTOQUE
        function openStockModal(id = null) {
            if (id) {
                const item = db.stock.find(s => s.id === id);
                if (item) {
                    document.getElementById('stock-modal-title').textContent = 'Editar Item';
                    document.getElementById('stock-id').value = item.id;
                    document.getElementById('stock-name').value = item.name;
                    document.getElementById('stock-quantity').value = item.quantity;
                    document.getElementById('stock-minimum').value = item.minimum || 0;
                    document.getElementById('stock-category').value = item.category;
                    document.getElementById('btn-delete-stock').classList.remove('hidden');
                }
            } else {
                document.getElementById('stock-modal-title').textContent = 'Novo Item no Estoque';
                document.getElementById('stock-form').reset();
                document.getElementById('stock-id').value = '';
                document.getElementById('btn-delete-stock').classList.add('hidden');
            }
            openModal('stockModal');
        }

        function submitStockItem(e) {
            e.preventDefault();
            try {
                const id = document.getElementById('stock-id').value;
                
                let item = {
                    id: id || getID(),
                    name: document.getElementById('stock-name').value,
                    quantity: parseInt(document.getElementById('stock-quantity').value) || 0,
                    minimum: parseInt(document.getElementById('stock-minimum').value) || 0,
                    category: document.getElementById('stock-category').value,
                    lastUpdated: new Date().toISOString()
                };

                if (id) {
                    const index = db.stock.findIndex(s => s.id === id);
                    if (index !== -1) {
                        item.createdAt = db.stock[index].createdAt; // Preserve creation date
                        db.stock[index] = item;
                    }
                } else {
                    item.createdAt = new Date().toISOString(); // Add creation date for new items
                    db.stock.push(item);
                }

                save();
                closeModal('stockModal');
                renderStock();
                showNotification('Item do estoque salvo com sucesso!', 'success');
            } catch (error) {
                console.error(error);
                showNotification('Erro ao salvar item: ' + error.message, 'error');
            }
        }

        function deleteStockItem() {
            const id = document.getElementById('stock-id').value;
            if (confirm('Tem certeza que deseja excluir este item do estoque?')) {
                db.stock = db.stock.filter(s => s.id !== id);
                save();
                closeModal('stockModal');
                renderStock();
                showNotification('Item excluído com sucesso!', 'success');
            }
        }

        function renderStock() {
            const container = document.getElementById('stock-items');
            const lowStockItems = db.stock.filter(item => item.quantity <= (item.minimum || 0));

            document.getElementById('low-stock-count').innerText = `${lowStockItems.length} itens precisam de reposição`;

            if (db.stock.length === 0) {
                container.innerHTML = `
                    <div class="col-span-3 bg-white p-8 rounded-2xl text-center border border-gray-100">
                        <i data-lucide="package" class="w-12 h-12 mx-auto mb-4 text-gray-300"></i>
                        <h3 class="text-lg font-bold text-gray-800 mb-2">Estoque vazio</h3>
                        <p class="text-gray-500 mb-4">Comece cadastrando seus materiais e medicamentos</p>
                        <button onclick="openStockModal()" class="bg-plena-blue text-white px-4 py-2 rounded-lg font-bold hover:bg-plena-dark">
                            Adicionar Primeiro Item
                        </button>
                    </div>
                `;
                return;
            }

            container.innerHTML = db.stock.map(item => `
                <div class="bg-white p-6 rounded-2xl border ${item.quantity <= (item.minimum || 0) ? 'border-red-200' : 'border-gray-100'} shadow-sm">
                    <div class="flex justify-between items-start mb-4">
                        <div>
                            <h3 class="font-bold text-gray-800">${item.name}</h3>
                            <p class="text-xs text-gray-500">${item.category}</p>
                        </div>
                        <span class="text-xs px-3 py-1 rounded-full ${item.quantity <= (item.minimum || 0) ? 'bg-red-100 text-red-700' : 'bg-green-100 text-green-700'}">
                            ${item.quantity <= (item.minimum || 0) ? 'Baixo' : 'OK'}
                        </span>
                    </div>
                    <div class="mb-4">
                        <div class="flex justify-between text-sm mb-1">
                            <span class="text-gray-500">Quantidade</span>
                            <span class="font-bold ${item.quantity <= (item.minimum || 0) ? 'text-red-600' : 'text-gray-800'}">${item.quantity}</span>
                        </div>
                        <div class="progress-bar">
                            <div class="progress-fill ${item.quantity <= (item.minimum || 0) ? 'bg-red-500' : 'bg-green-500'}" 
                                 style="width: ${Math.min((item.quantity / (item.minimum || 1)) * 100, 100)}%"></div>
                        </div>
                    </div>
                    <div class="flex justify-between items-center">
                        <span class="text-xs text-gray-500">Mínimo: ${item.minimum || 0}</span>
                        <div class="flex gap-2">
                            <button onclick="updateStock('${item.id}', 1)" class="text-xs px-2 py-1 bg-blue-100 text-blue-700 rounded hover:bg-blue-200">
                                +1
                            </button>
                            <button onclick="updateStock('${item.id}', -1)" class="text-xs px-2 py-1 bg-gray-100 text-gray-700 rounded hover:bg-gray-200">
                                -1
                            </button>
                            <button onclick="openStockModal('${item.id}')" class="text-xs px-2 py-1 bg-gray-100 text-gray-700 rounded hover:bg-gray-200">
                                Editar
                            </button>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function updateStock(id, delta) {
            const item = db.stock.find(s => s.id === id);
            if (!item) return;

            item.quantity = Math.max(0, item.quantity + delta);
            item.lastUpdated = new Date().toISOString();

            save();
            renderStock();
            showNotification(`Estoque atualizado: ${item.name}`, 'success');
        }

        // RELATÓRIOS
        function generateReport(type) {
            let reportData = '';

            if (type === 'monthly') {
                const currentMonth = new Date().toISOString().slice(0, 7);
                const monthIncome = db.financial.filter(f => f.date.startsWith(currentMonth) && f.type === 'income')
                    .reduce((sum, f) => sum + f.amount, 0);
                const monthExpense = db.financial.filter(f => f.date.startsWith(currentMonth) && f.type === 'expense')
                    .reduce((sum, f) => sum + f.amount, 0);

                reportData = `RELATÓRIO MENSAL - ${currentMonth}\n\n` +
                    `Receitas: ${fmtMoney(monthIncome)}\n` +
                    `Despesas: ${fmtMoney(monthExpense)}\n` +
                    `Saldo: ${fmtMoney(monthIncome - monthExpense)}\n\n` +
                    `Total de Pacientes: ${db.patients.length}\n` +
                    `Consultas Realizadas: ${db.appointments.filter(a => a.status === 'completed').length}\n` +
                    `Tratamentos Ativos: ${db.treatments.filter(t => t.status === 'active').length}`;
            } else if (type === 'treatments') {
                reportData = `RELATÓRIO DE TRATAMENTOS\n\n`;
                db.treatments.forEach(t => {
                    reportData += `${getPatientName(t.patientId)} - ${t.type}\n` +
                        `Valor: ${fmtMoney(t.value)} | Progresso: ${calculateProgress(t)}%\n` +
                        `Sessões: ${t.completedSessions || 0}/${t.totalSessions}\n` +
                        `Status: ${t.status === 'active' ? 'Ativo' : 'Concluído'}\n\n`;
                });
            }

            alert(reportData);
        }

        // CONFIGURAÇÕES
        function saveSettings() {
            db.settings.clinicName = document.getElementById('clinic-name').value;
            db.settings.dentistCRO = document.getElementById('dentist-cro').value;
            db.settings.clinicPhone = document.getElementById('clinic-phone').value;
            db.settings.appointmentDuration = parseInt(document.getElementById('appointment-duration').value) || 30;
            db.settings.reminderHours = parseInt(document.getElementById('reminder-hours').value) || 24;

            save();
            showNotification('Configurações salvas com sucesso!', 'success');
        }

        // BACKUP E RESTAURAÇÃO
        function downloadBackup() {
            const dataStr = JSON.stringify(db, null, 2);
            const dataUri = 'data:application/json;charset=utf-8,' + encodeURIComponent(dataStr);
            const exportFileDefaultName = `plena_odonto_backup_${new Date().toISOString().split('T')[0]}.json`;

            const linkElement = document.createElement('a');
            linkElement.setAttribute('href', dataUri);
            linkElement.setAttribute('download', exportFileDefaultName);
            linkElement.click();

            showNotification('Backup exportado com sucesso!', 'success');
        }

        function restoreBackup(input) {
            const file = input.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function (e) {
                try {
                    const restoredData = JSON.parse(e.target.result);
                    if (confirm('Tem certeza que deseja restaurar este backup? Todos os dados atuais serão substituídos.')) {
                        db = restoredData;
                        save();
                        location.reload();
                    }
                } catch (error) {
                    alert('Erro ao ler o arquivo de backup. Verifique se o arquivo é válido.');
                }
            };
            reader.readAsText(file);
        }

        function clearAllData() {
            if (confirm('ATENÇÃO: Isso irá apagar TODOS os dados (pacientes, agendamentos, tratamentos, etc.). Tem certeza?')) {
                db = JSON.parse(JSON.stringify(defaultDB));
                save();
                location.reload();
            }
        }

        function factoryReset() {
            if (confirm('ATENÇÃO: Isso irá resetar completamente o sistema. Todos os dados serão perdidos. Tem certeza?')) {
                localStorage.removeItem(DB_KEY);
                location.reload();
            }
        }

        // NOTIFICAÇÕES
        // NOTIFICAÇÕES
        function showNotification(message, type = 'info') {
            const colors = {
                'success': 'bg-green-500',
                'error': 'bg-red-500',
                'info': 'bg-blue-500',
                'warning': 'bg-amber-500'
            };

            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 ${colors[type] || 'bg-gray-800'} text-white px-6 py-3 rounded-xl shadow-lg transform transition-all duration-300 translate-y-[-100%] opacity-0 z-50 flex items-center gap-3 font-medium`;
            notification.innerHTML = `
                <i data-lucide="${type === 'success' ? 'check-circle' : type === 'error' ? 'alert-circle' : 'info'}" class="w-5 h-5"></i>
                ${message}
            `;

            document.body.appendChild(notification);

            if (window.lucide) lucide.createIcons();

            // Animate in
            requestAnimationFrame(() => {
                notification.classList.remove('translate-y-[-100%]', 'opacity-0');
            });

            // Remove after 3s
            setTimeout(() => {
                notification.classList.add('translate-y-[-100%]', 'opacity-0');
                setTimeout(() => {
                    if (document.body.contains(notification)) {
                        document.body.removeChild(notification);
                    }
                }, 300);
            }, 3000);
        }

        // MANUAL DE USO
        function initManual() {
            // Carregar progresso
            const savedProgress = JSON.parse(localStorage.getItem('odonto_manual_progress') || '{}');

            document.querySelectorAll('.manual-check').forEach(check => {
                const section = check.dataset.section;
                check.checked = !!savedProgress[section];
            });

            updateManualProgress();

            if (window.lucide) lucide.createIcons();
        }

        function toggleManualSection(header) {
            const content = header.nextElementSibling;
            const icon = header.querySelector('[data-lucide="chevron-down"]');

            content.classList.toggle('hidden');
            if (content.classList.contains('hidden')) {
                icon.style.transform = 'rotate(0deg)';
            } else {
                icon.style.transform = 'rotate(180deg)';
            }
        }

        function updateManualProgress() {
            const checks = document.querySelectorAll('.manual-check');
            const total = checks.length;
            const checked = Array.from(checks).filter(c => c.checked).length;
            const percentage = Math.round((checked / total) * 100);

            document.getElementById('manual-progress-text').innerText = `${percentage}% Concluído`;
            document.getElementById('manual-progress-bar').style.width = `${percentage}%`;

            // Salvar no localStorage
            const progress = {};
            checks.forEach(c => {
                progress[c.dataset.section] = c.checked;
            });
            localStorage.setItem('odonto_manual_progress', JSON.stringify(progress));

            if (percentage === 100) {
                showNotification('Parabéns! Você completou o treinamento.', 'success');
            }
        }

        function scrollToManualSection(id) {
            const el = document.getElementById(id);
            if (el) {
                el.scrollIntoView({ behavior: 'smooth', block: 'center' });
                // Abrir a seção se estiver fechada
                const content = el.querySelector('.hidden');
                if (content) {
                    toggleManualSection(el.querySelector('.cursor-pointer'));
                }
            }
        }

        function resetManualProgress() {
            if (confirm('Deseja reiniciar o progresso do manual?')) {
                localStorage.removeItem('odonto_manual_progress');
                document.querySelectorAll('.manual-check').forEach(c => c.checked = false);
                updateManualProgress();
                showNotification('Progresso reiniciado', 'info');
            }
        }

        // INICIALIZAR APLICATIVO
        document.addEventListener('DOMContentLoaded', init);
    </script>
</body>

</html>
